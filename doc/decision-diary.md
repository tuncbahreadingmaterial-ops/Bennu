# Bennu Decision Diary

Record material design and implementation decisions here as they are made. Keep this diary append-only: add a new entry to correct or supersede an earlier decision instead of silently rewriting history.

## Entry format

### YYYY-MM-DD — Short decision title

- **Related issue:** Link or identifier
- **Context:** The problem, constraints, and relevant facts.
- **Decision:** What was selected, including its scope.
- **Alternatives considered:** Other credible options and why they were not selected.
- **Rationale:** Why this choice best fits the current constraints.
- **Consequences or follow-up:** Expected effects, risks, and any later work.
- **Validation/evidence:** Tests, measurements, review evidence, or other checks supporting the decision.

## Entries

### 2026-07-18 — Establish a minimal, normative contributor contract

- **Related issue/PR:** [Issue #1](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/1)
- **Context:** Bennu had no tracked files or implementation baseline. Before implementation begins, contributors need one repository-root contract that preserves the product direction, C++ architecture constraints, explicit error handling, scoped issue workflow, and ongoing decision record required by Issue #1. The issue explicitly excludes choices about syntax, execution model, C++ standard, build system, supported platforms, dependencies, and compiler or interpreter implementation.
- **Decision:** Bootstrap the repository with only root-level `AGENTS.md` and this diary. State the issue's contributor rules normatively and group them by product direction, C++ and architecture constraints, and engineering workflow. Treat Anka as linked design inspiration rather than a compatibility target. Use one append-only diary with a compact, reusable field-based entry format, and deliberately defer every listed non-goal.
- **Alternatives considered:** Copy Anka's architecture or behavior; add a build system or implementation skeleton; replace explicit constraints with generic style guidance; or create separate ADR files for each decision. These options were rejected because they would impose compatibility not requested by the issue, introduce speculative scaffolding, weaken discoverability and enforcement, or add unnecessary process for the project's current size.
- **Rationale:** Two focused Markdown files are the smallest complete bootstrap. A root `AGENTS.md` is automatically discoverable by contributors and agents, while a single diary makes decisions chronological and easy to inspect without selecting product or implementation details prematurely.
- **Consequences or follow-up:** Future contributions must follow `AGENTS.md`, document intentional differences from Anka, and append diary entries for material choices. Syntax, execution strategy, toolchain, platforms, dependencies, and implementation remain undecided until issue-driven work selects them. If a recorded decision changes, the correction must be a new entry.
- **Validation/evidence:** Both files were read back from the repository root and checked directly against every acceptance criterion in Issue #1. The complete untracked-file list contained only `AGENTS.md` and `doc/decision-diary.md`; inspection confirmed there is no compiler, interpreter, or unrelated scaffolding.

### 2026-07-18 — Adopt issue branches and local-main integration without pull requests

- **Related issue:** [Issue #3](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/3)
- **Context:** Bennu's contributor contract required issue-driven, scoped work but did not define how Coder isolates, integrates, or hands off an issue. The project needs a predictable path that gives Chef an already-integrated local result to verify while avoiding a GitHub pull-request review loop. A shared `main` also makes unconstrained concurrent integration unsafe.
- **Decision:** For every issue, Coder starts from a clean, current local `main`; creates one `issue/<number>-<short-slug>` branch; keeps scoped, issue-referencing commits on that branch; and runs all required validation there. After validation passes, Coder merges the branch into local `main`, pushes `main` to `origin`, leaves the GitHub issue open with exact branch, commit, merged-main, changed-file, push, and validation evidence, and blocks the matching Kanban card as `review-required`. Bennu does not use GitHub pull requests or GitHub code review. Unsafe integration stops and blocks with evidence. One active Coder issue is the default unless Chef explicitly establishes independent integration as safe.
- **Alternatives considered:** Use GitHub pull requests and reviews; commit issue work directly to `main`; leave validated issue branches unmerged for Chef; or allow multiple Coders to merge concurrently by default. Pull requests add a review mechanism the project deliberately does not want, direct commits remove issue-level isolation, unmerged branches shift integration work to verification, and default concurrency risks conflicts or remote divergence on the shared integration branch.
- **Rationale:** A short-lived issue branch provides isolation and auditable commits, while local merge plus a pushed `main` gives Chef one stable integrated range to inspect. Explicit evidence and stop conditions preserve independent verification without disguising integration as acceptance or forcing unsafe Git operations.
- **Consequences or follow-up:** Coder work is normally serialized. Local merge does not close the issue or accept the result; Chef still inspects the merged range, reruns tests, exercises the product, and either accepts the work or returns the same issue and card with exact unmet criteria. A dirty worktree, unexpected remote divergence, conflict, failed validation, or unrelated diff blocks integration until resolved.
- **Validation/evidence:** Issue #3 exercises this policy itself on `issue/3-local-main-workflow`. Validation reads both changed files from the repository root, checks the acceptance criteria and normative text, confirms only `AGENTS.md` and `doc/decision-diary.md` changed, inspects issue-referencing history and matching branch/main/origin SHAs, verifies fresh Hermes context discovery of root `AGENTS.md`, and confirms no pull request exists for the issue.

### 2026-07-18 — Establish the portable build, CLI contract, tests, and main CI

- **Related issue:** [Issue #4](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/4)
- **Context:** Bennu had no build configuration, executable, automated tests, CI, license, or clean-checkout instructions. This first runnable foundation must work across the Level 1 release platforms without introducing evaluator, backend, packaging, or speculative framework code. Anka currently builds through a Visual Studio 2022 solution with vcpkg and exposes `-r` and `-f`; Bennu needs a deliberately portable baseline and the named command surface selected by Issue #4 instead.
- **Decision:** Require CMake 3.20, Ninja, and C++20 without compiler extensions, and build one standard-library-only `bennu` executable from `src/main.cpp`. Generate a build-tree identity record containing the selected system, processor, compiler, and CMake version. Reserve `repl`, `run`, `emit-c`, and `build` as explicit subcommands that fail until later issues implement them. Use CTest plus a CMake script to execute the built binary and compare process status, stdout, and stderr for help, missing arguments, unknown input, and every reserved command; this creates no separate test executable or runtime source-tree dependency. Run the same Release configure, build, and CTest commands on pushes to `main` and manual dispatches on fixed Linux x64, Windows x64, and macOS arm64 runner labels. Each job records its observed OS and CPU architecture, fails on a matrix mismatch, and reports compiler, CMake, and Ninja identities. Pin the checkout and Windows MSVC-environment actions to commits.
- **Alternatives considered:** Copy Anka's Visual Studio solution and vcpkg baseline; use compiler-specific project files; add a third-party CLI or test framework; use shell-specific test scripts; implement the reserved commands now; or run CI for pull requests. These options were rejected because they reduce portability, add dependencies or runtime behavior outside this issue, make process assertions less uniform, or contradict Bennu's local-main workflow.
- **Rationale:** One executable, one small process-test driver, and one three-runner CI matrix are the smallest complete foundation. Explicit C++ standard requirements and pinned, versioned runner inputs make costs and platform behavior visible. Recognizing reserved commands without implementing them stabilizes the user-facing shape while keeping the language entirely absent.
- **Consequences or follow-up:** Later issues can replace each reserved failure with real behavior while retaining its process contract. The CLI currently performs no source I/O and needs no files beside the executable. The integrated `main` CI run remains the required cross-platform gate; runner-label or image retirement will require a new diary entry and workflow update.
- **Validation/evidence:** During test-driven implementation, each of the eight process contracts was observed failing before its matching behavior and then passing in `/tmp/bennu-tdd-build`. A clean GNU 15.2.0 Release configure/build passed all 8 CTest cases with `-std=c++20`; the generated identity recorded Linux x86_64, GNU 15.2.0, and CMake 4.4.0. The copied executable passed help and all failure contracts from outside the source tree, a warning-clean compile passed, and actionlint 1.7.12 accepted the workflow. The integrated three-platform GitHub Actions run remains required before Issue #4 handoff.

### 2026-07-18 — Use flat Level 1 programs, explicit results, and bounded arrays

- **Related issue:** [Issue #5](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/5)
- **Context:** The first language core must provide one reusable source-to-value path for decimal integers, `inc`, and `ioata` without coupling semantics to the CLI or later backends. It must preserve useful source locations, reject signed overflow and malformed input explicitly, and prevent a small untrusted request from causing an uncontrolled array allocation. Anka is semantic inspiration, but its broader grammar and array lifting are outside Level 1.
- **Decision:** Represent Level 1 integers with `std::int64_t` and convert literals with non-throwing `std::from_chars`. Represent a value as a public tag plus scalar field and contiguous `std::vector<std::int64_t>` array field. Tokens store byte offsets, one-based lines and columns, lengths, and whether horizontal whitespace preceded them. Parse each nonblank line into one contiguous postorder node array plus source-ordered root indices; evaluate that array in one forward pass rather than building an owning pointer tree or recursively evaluating calls. Expose tokenization, parsing, primitive application, complete-program evaluation, one-expression evaluation, and formatting as free functions with boolean result records carrying structured errors. Limit `ioata` to 1,000,000 elements, checked before `reserve`, and apply the same 1,000,000-element cumulative budget to all `ioata` results retained by one program evaluation. `ioata` of a nonpositive integer produces an empty vector without reserving, and `inc` rejects arrays rather than lifting over them.
- **Alternatives considered:** Use machine-width integers; use exceptions or add an expected/variant dependency; represent each value as a polymorphic object; build a recursive owning syntax tree; recursively evaluate nested calls; allow each program line its own independent million-element budget; or copy Anka's `inc` array lifting and broader syntax. These options were rejected because they weaken cross-platform semantics, violate explicit-error or data/behavior separation constraints, add dependencies or pointer ownership, make deep prefix programs consume the call stack, permit large amplification across a small multi-line source, or expand the specified language.
- **Rationale:** The tagged value keeps scalar access direct and array storage contiguous. Postorder nodes make every argument an earlier index, so ownership and evaluation order remain visible and nested calls have predictable linear traversal. A million signed 64-bit elements has an 8,000,000-byte (about 7.63 MiB) raw payload; checking both each request and the retained program total before allocation gives a deterministic, testable bound while remaining large enough for Level 1 examples. Explicit result records keep all expected failures in ordinary control flow and make line-and-column reporting available to later presentation layers.
- **Consequences or follow-up:** The inactive field in each tagged value is intentional small structural overhead. Tokens refer to source ranges by offset, so callers pass the same source buffer to parsing but tokens do not retain borrowed string views. Program evaluation retains intermediate values until the forward pass finishes; the cumulative array budget bounds that storage. Columns count source bytes/code units, which is sufficient because Level 1 tokens are ASCII. Later CLI or backend issues should consume this API rather than duplicate grammar or semantics, and any future Unicode, wider value model, lifting, or different resource policy requires a new language decision.
- **Validation/evidence:** Test-first cycles observed missing API or behavior failures before scalar evaluation, signed boundaries, `inc`, `ioata`, formatting, complete-program ordering, token locations, flat parsing, direct primitive errors, and the cumulative allocation guard were implemented. The focused evaluator test then passed with exact scalar/array formatting, nested calls, LF/CRLF/blank-line handling, missing final newline, every required malformed/type/range/allocation error class, exact locations, and both per-request and cumulative 1,000,000-element boundaries. A clean GNU 15.2.0 Release build passed all 9 CTest cases, the evaluator case passed under combined AddressSanitizer and UndefinedBehaviorSanitizer instrumentation, and a `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror` build was clean. Integrated Linux, Windows, and macOS CI remains required before handoff.

### 2026-07-18 — Validate public evaluator data before transformation

- **Related issue:** [Issue #5](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/5)
- **Context:** The Level 1 API deliberately exposes plain token and expression structs. Callers can therefore construct values that the tokenizer and parser would never emit. Review found that unchecked token spans could overflow before substring access, unknown expression tags could select the wrong primitive path, and incomplete token streams could silently ignore source bytes or report misleading end locations.
- **Decision:** Treat every caller-supplied `Token` and `Program` as untrusted structural input. Validate token spans with subtraction after proving the offset is in range, reject unknown token and expression tags, require exactly one final zero-length end token at `source.size()`, and reject tokens following any earlier end token. When an end token is missing, derive its error location by scanning the source to the actual byte, line, and column at EOF rather than trusting the last supplied token.
- **Alternatives considered:** Document that only tokenizer output may be parsed; use `offset + length` range checks; let unknown enum tags fall through existing branches; accept an end token anywhere in the source; or report the last token location for a missing end marker. These options were rejected because public data remains constructible, addition can wrap, fallthrough changes semantics, an early end can hide source text, and the last token start is not the source end.
- **Rationale:** One linear validation pass keeps failure behavior explicit and bounded before parsing touches source ranges or evaluates public nodes. Requiring the end marker to describe EOF preserves the parser's complete-buffer contract, while source-derived EOF locations make diagnostics deterministic even when the token vector is empty or truncated.
- **Consequences or follow-up:** Parsing public tokens performs an additional linear validation pass and an EOF scan only on the missing-end error path. Token line and column fields remain caller-supplied diagnostic metadata; this change validates memory safety, known tags, and stream termination without duplicating tokenization or adding a second token representation.
- **Validation/evidence:** Focused regressions were observed failing before the corresponding corrections for overflow-sized spans, unknown expression tags, nonzero-length and early end tokens, and missing-end location handling. The evaluator test now covers malformed public spans and tags, empty and truncated token streams, tokens after an early end, a final end before source EOF, nonzero-length end tokens, and exact LF/CRLF-aware multiline EOF locations. Full Release, strict no-exceptions/warnings, sanitizer, adversarial, and integrated CI gates remain required before handoff.

### 2026-07-18 — Require exact token and source correspondence

- **Related issue:** [Issue #5](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/5)
- **Context:** The prior public-data validation checked token bounds, known tags, and final end-marker placement independently, but it did not prove that the ordered tokens described the supplied complete source. A caller could omit non-whitespace bytes, overlap or reorder spans, forge a primitive kind for a different lexeme, or supply false whitespace and location metadata. The parser could then accept and evaluate semantics that were not present in the source buffer.
- **Decision:** Retain the public `parse_program(source, tokens)` staged API, but treat `tokenize(source)` as the canonical complete lexical description. After the existing overflow-safe structural checks and before building any expression nodes, tokenize the source independently and require the supplied vector to match the canonical vector exactly in count, order, kind, byte offset, byte length, line, column, and separation flag. Reject every mismatch as a located `invalid_program` error; if the source itself cannot tokenize, reject the supplied stream at the canonical lexical-error location.
- **Alternatives considered:** Validate only monotonic spans and non-whitespace gaps; duplicate selected lexeme checks inside the parser; document that only tokenizer-produced vectors are supported; or remove the token-vector parser overload. Partial span or lexeme checks can drift from tokenizer rules and leave metadata forgery paths, documentation cannot constrain constructible public data, and removing the overload would unnecessarily break the staged API required by Issue #5.
- **Rationale:** Exact comparison against the existing tokenizer fixes the entire correspondence class with one source of lexical truth and no second scanner or token representation. Keeping the preliminary structural pass preserves safe handling and useful diagnostics for overflow-sized spans and malformed enum tags before comparison accesses public ranges.
- **Consequences or follow-up:** Public parsing now performs a second linear lexical pass and temporary token-vector allocation; this is the explicit cost of retaining an untrusted token-vector contract without credentials or hidden provenance. The normal source evaluator remains semantically unchanged. Any future tokenizer field or lexical rule automatically becomes part of the parser correspondence contract and must keep tokenizer and parser tests in sync.
- **Validation/evidence:** Before implementation, focused regressions reproduced all four rejected classes: omitted non-whitespace source, overlapping spans, out-of-order spans, and a forged `inc` kind over the lexeme `xxxxx`. The focused evaluator test reported all four failures against the integrated implementation, then passed after exact correspondence validation was added. Full Release, strict no-exceptions/warnings, sanitizer/adversarial, hygiene, and integrated CI gates remain required before handoff.

### 2026-07-18 — Use dependency-gated implementation and verification cards

- **Related issue:** [Issue #11](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/11)
- **Context:** Bennu's issue-branch workflow integrated Coder changes into `main` and then blocked the same Kanban card as `review-required`. Although terminal events notified Telegram, an outgoing notification did not dispatch Chef; independent verification therefore waited for a user reply or the three-hour stewardship poll. Issue #6 and later implementation work needs an immediate native handoff without weakening independent acceptance or the shared-main serialization rule.
- **Decision:** Represent each implementation issue from Issue #6 onward as `Implement #<number> (coder) -> Verify #<number> (chef)`, with the Chef card parent-gated on the Coder card. Coder completes only the implementation stage after the issue branch is validated, merged to local `main`, pushed, green on required integrated-main CI, and documented in a durable GitHub handoff while the issue remains open. Chef then independently verifies the merged range, acceptance criteria, runtime behavior, adjacent and error cases, and CI. Chef alone closes the issue and completes verification after recording evidence. A failed verification keeps or reopens the issue and creates a focused Coder correction card followed by a dependency-gated Chef re-verification card; failed work is never marked accepted. Keep one active Coder integration at a time, and permit concurrent Chef verification only in an isolated clean checkout that cannot disturb shared state. Retain Telegram terminal-event subscriptions and the three-hour stewardship cron only as notification and reconciliation safeguards.
- **Alternatives considered:** Keep blocking a single card for review; rely on Telegram replies or shorten the stewardship cron; add a custom task-event daemon; let Coder mark the issue accepted after CI; or allow Chef to inspect the shared checkout while another Coder integrates. These options were rejected because they preserve manual or polling latency, add operational machinery instead of using native dependencies, collapse implementation and acceptance, or risk cross-worker corruption and verification of moving state.
- **Rationale:** A parent edge is the smallest durable trigger the dispatcher already understands: implementation completion makes exactly one verifier ready immediately. Separate cards preserve role ownership and auditable state, while correction and re-verification stages represent failure honestly. Requiring integrated-main CI before Coder completion gives Chef a stable candidate, and checkout isolation preserves both serialization and independent evidence.
- **Consequences or follow-up:** This replaces the legacy single-card `review-required` convention for Issue #6 and later implementation work after Issue #11 is independently accepted. Orchestration must create both cards with the correct assignees and parent edge before dispatch and must avoid duplicate runnable implementation cards. Chef will validate the first Issue #6 graph after accepting this transition. Existing subscriptions and stewardship continue to detect or reconcile missed terminal events but are not part of the normal handoff path. This issue changes no Bennu product, build, test, release, daemon, cron, or Level 2 behavior.
- **Validation/evidence:** The workflow text is checked directly against every Issue #11 acceptance criterion, including implementation completion gates, open-issue ownership, independent verification evidence, failure routing, shared-main serialization, reviewer isolation, and safeguard roles. Repository validation and required integrated-main CI remain required before handoff; the first Issue #6 graph is intentionally deferred until independent Chef acceptance of Issue #11.

### 2026-07-18 — Terminalize rejected verification attempts without accepting product work

- **Related issue:** [Issue #11](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/11)
- **Context:** The initial two-stage workflow required a failed Chef verifier to remain incomplete while also requiring a parent-gated Coder correction. Those requirements deadlock the native dependency graph: the correction cannot become ready until its failed-verifier parent is terminal, but the verifier was forbidden from completing. The contract also needs to distinguish completion of a rejected verification attempt from acceptance of the product work.
- **Decision:** On verification failure, preserve or reopen the GitHub issue and record exact rejection evidence. Create the retry graph with explicit `failed verifier -> Coder correction -> Chef re-verification` parent edges. After both downstream task IDs exist, complete the failed verification-attempt card with structured metadata containing `accepted: false` and both task IDs. Treat that completion only as terminalization of the rejected attempt; only a later successful Chef verifier may record product acceptance and close the GitHub issue.
- **Alternatives considered:** Leave the failed verifier blocked and create an unparented correction; make the correction depend only on the original implementation; reuse the failed verifier for another run; or complete it without structured rejection metadata. These options were rejected because they deadlock the approved graph, bypass the failed-verifier handoff, blur distinct verification attempts, or make a terminal rejected attempt indistinguishable from acceptance.
- **Rationale:** Explicit parent edges preserve native dispatcher sequencing, while `accepted: false` records the outcome honestly and lets the failed attempt become terminal without claiming success. Separate correction and re-verification cards keep implementation and independent acceptance owned by the correct roles and make every retry auditable.
- **Consequences or follow-up:** Failed verification cards are terminal records, not successful product stages. Their metadata must identify the two retry cards, the GitHub issue remains open, and acceptance belongs exclusively to a later successful Chef verifier. The same correction-stage integration gates and re-verification evidence requirements continue to apply; no product, build, CI, daemon, cron, or Level 2 behavior changes.
- **Validation/evidence:** Chef independently reproduced the deadlock in the initial contract after integrated Main CI and Release CTest passed, and recorded the gap at [Issue #11 verification evidence](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/11#issuecomment-5012779523). Validation for this correction checks the exact three-stage retry edges, structured `accepted: false` completion, later-verifier-only acceptance, append-only diary history, repository tests, and integrated three-platform Main CI.

### 2026-07-19 — Reuse one evaluator path for recoverable REPL and atomic file execution

- **Related issue:** [Issue #6](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/6)
- **Context:** Bennu's accepted Level 1 evaluator returned structured values and located errors but had no user-facing execution path. The first CLI journey needs exact prompts and value prefixes, independent recovery after each bad interactive line, complete-file failure without partial stdout, portable source loading, and diagnostics that remain useful without C++ exception output.
- **Decision:** Link the existing `bennu_core` into the executable and keep CLI behavior as plain data plus free functions in `src/main.cpp`. The REPL flushes `> ` before each read, ignores horizontal-whitespace-only lines, evaluates each other line independently with `evaluate_expression`, emits successful values as `>>` plus `format_value`, and reports a failed line before continuing. A prompt already written before EOF remains the final stdout text and EOF returns status 0. The file runner reads the named path in binary mode through a fixed-size C stdio buffer and an explicit result record, then calls `evaluate_source` once and writes formatted values only after the complete result succeeds. Diagnostics use `source:line:column: category: message`; file-open and file-read failures use the same concise located file-error form.
- **Alternatives considered:** Build a second CLI parser; keep state across REPL lines; evaluate a file line by line and stream each early success; load files with `std::istreambuf_iterator`; print diagnostics on stdout; or add a CLI framework. These were rejected because they duplicate semantics, introduce Level 2 state, permit partial batch output, can surface an implementation-specific `std::__ios_failure` dump for directory/read failures, break machine-stable result streams, or add dependencies and abstractions for two commands.
- **Rationale:** One evaluator and formatter path keeps language behavior identical in library, REPL, and file use. Per-line REPL calls naturally isolate errors, while one complete-file call preserves all-or-nothing stdout. Fixed-buffer stdio makes open, read, and close failures visible in ordinary control flow and avoids the observed exception-based stream failure without adding a dependency.
- **Consequences or follow-up:** `emit-c`, `build`, persistent REPL context, history, completion, color, meta-commands, stdin batch execution, and later language levels remain unimplemented. Windows text-mode stdout may encode logical newlines as CRLF; source files are read byte-for-byte so the evaluator's existing LF/CRLF rules remain authoritative. Future source encoding or file-size policies require a separate decision.
- **Validation/evidence:** Test-first process cases observed the reserved-command failures before the canonical runner and REPL were implemented, missing diagnostic categories before recovery diagnostics were completed, blank-line errors before blank lines were ignored, argument-contract failures before exact dispatch was added, and file-error mismatches before explicit file loading replaced stream iteration. A directory reproduction previously aborted with `std::__ios_failure`; the explicit loader returns a concise nonzero file error instead. The Debug build's full CTest suite passes 22 tests, including real-executable transcripts, recovery, EOF, golden files, paths with spaces, LF/CRLF and blank lines, file failures, every source-reachable evaluator error class, and zero partial batch stdout. Clean Release, strict, sanitizer, and integrated CI gates remain required before handoff.

### 2026-07-19 — Emit validated Level 1 results as compact self-contained C11

- **Related issue:** [Issue #7](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/7)
- **Context:** Bennu needs a deterministic portable-C boundary for every accepted Level 1 program without adding a second grammar, a runtime dependency, or C compiler invocation. Emission must preserve the evaluator's signed 64-bit, ordering, overflow, type, formatting, and cumulative one-million-element allocation semantics, while an output failure must never truncate an existing destination. The maximum safe `ioata` output is several megabytes, so embedding a fully formatted output string would make generated source scale with result size.
- **Decision:** Add one free-function C emitter that calls the accepted `evaluate_source` pipeline before producing any C bytes. Use validated constant folding: emit each scalar root as an exact `INT64_C` expression (with a portable two-term expression for `INT64_MIN`) and each array root as its validated element count. Generate only the C11 standard headers and formatting helper functions needed by that program; array output is a bounded loop, so generated source stays compact and performs no dynamic allocation. Preserve root order in `main`, use `PRId64` for portable formatting, and return nonzero if generated-program stdout fails. Keep C compiler discovery and invocation exclusively in CTest, never in the `bennu` executable. Write output to an exclusively created same-directory sibling, close it successfully, then atomically replace the destination with POSIX `rename` or Windows `MoveFileEx` replacement; remove the sibling on every failure.
- **Alternatives considered:** Reparse source in a C backend; lower the public flat nodes into a second runtime evaluator; emit one giant escaped output string; write the destination directly; remove an existing Windows destination before a portable rename; or bundle/invoke a compiler. A second parser or runtime evaluator can drift from accepted semantics, giant strings scale source size with `ioata`, direct writes corrupt sentinels on late failure, remove-then-rename creates a data-loss window, and compiler invocation belongs to Issue #8.
- **Rationale:** Validated constant folding is the smallest complete backend for a side-effect-free Level 1 language. It makes every source/type/range/allocation error occur before output replacement, while compact scalar/count data and fixed helpers make the artifact inspectable, deterministic, standard-library-only, and independent of Bennu at runtime. Same-directory exclusive temporary creation keeps replacement on one filesystem and prevents collisions from overwriting unrelated files.
- **Consequences or follow-up:** Emission performs the evaluator's existing allocations before translation, including up to the documented cumulative one-million-element limit, but emitted C allocates no array storage and remains under a small fixed size plus one call per source result. Generated C is an executable implementation detail rather than a stable C API or optimized representation. Narrow command-line path handling remains consistent with the accepted runner; broader Unicode-path policy would require separate work. `bennu build`, compiler discovery/invocation, an external runtime ABI, optimization, packaging, and later language levels remain deferred.
- **Validation/evidence:** Test-first cycles observed the canonical `emit-c` journey fail as unimplemented, a direct destination write corrupt a sentinel in a non-writable directory, and empty-program C fail strict compilation because of unused helpers; each focused case passed after its corresponding implementation. Automated coverage inspects a committed golden C file, compiles and executes generated C11, compares direct and generated output through the maximum safe nested `ioata`, verifies signed boundaries/empty arrays/order/deterministic bytes/path independence, and exercises argument, input, semantic, output, collision, and atomic-preservation failures. Clean Release, strict warnings/conversions/Werror/no-exceptions, and ASan+UBSan builds each pass all 27 CTest cases. A detached canonical executable built with GNU C 15.2.0 runs after both C and temporary source files are removed and emits exactly `>>(1 2 3 4 5)` then `>>6`; duplicate emission hashes match and an invalid-source sentinel remains byte-identical with no orphan. Integrated three-platform CI remains required before handoff.

### 2026-07-19 — Invoke selected C compilers directly and publish native output last

- **Related issue:** [Issue #8](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/8)
- **Context:** Portable C emission exists, but the one-command native path must select an installed toolchain predictably, pass hostile path and environment text without shell interpretation, support GCC/Clang-style drivers and the Windows release `cl.exe` driver, and preserve an existing executable across source, discovery, process, compiler, temporary, and output failures.
- **Decision:** Add a plain-data native-build component with free functions for compiler selection, target command-line construction, direct process launch, and output publication. Selection is exact: nonempty `--cc`, then nonempty `CC`, then `cc` fallback discovery on Linux/macOS or `cl.exe` on Windows. Explicit and environment selections are final and are never retried through fallback. Pass GCC/Clang `-std=c11 <source> -o <output>` arguments or MSVC `/nologo /std:c11 <source> /Fe:<output>` arguments as an argument vector. On POSIX, use `fork` plus `execvp`; on Windows, use `CreateProcess` with Microsoft-compatible argument quoting. Redirect compiler output to a bounded diagnostic log without involving a command shell. Emit through the accepted evaluator-backed C emitter, compile only inside an exclusively created same-filesystem temporary directory, verify the compiler's zero status and regular output file, move the candidate out, clean the isolated directory, and only then atomically replace the requested destination.
- **Alternatives considered:** Invoke `system`, a shell, or a command string; parse flags from `CC`; silently try fallback after an explicit selection fails; use one GCC-style command on every platform; compile directly to the requested destination; or compile under a global temporary directory. These were rejected because they enable interpretation or ambiguous boundaries, expand the Level 1 interface, hide configuration errors, fail the native MSVC contract, corrupt or misrepresent failed outputs, or prevent same-filesystem atomic publication.
- **Rationale:** Direct OS process APIs keep compiler names and every path inert data. Separating deterministic command construction from process execution makes both driver branches inspectable, while same-directory isolation and publish-last ordering preserve output bytes for every pre-publication failure without bundling another toolchain or adding dependencies.
- **Consequences or follow-up:** Bennu captures at most the first 4096 bytes of combined compiler stdout/stderr for diagnostics. Windows narrow-path behavior remains consistent with the existing CLI; broader Unicode path policy is separate work. There is intentionally no flags, linker, cross-compilation, object/library, cache, download, or compiler-management interface. Compiler fallback availability remains an environment prerequisite.
- **Validation/evidence:** The first canonical real-compiler test failed against the reserved `build` command before implementation and then passed; a later regression test also reproduced relative compiler paths breaking after the isolated-directory `chdir` before path normalization fixed it. Deterministic fake-process coverage proves explicit/`CC`/fallback precedence, no fallback after selected failure, relative and metacharacter-containing compiler paths, GCC and MSVC argument boundaries, spaces and inert metacharacters, missing/non-executable/start/nonzero/termination/no-output failures, pre-compiler source rejection, sentinel and occupied-candidate preservation, no misleading new output, and temporary cleanup. Clean Release, strict warnings/conversions/Werror/no-exceptions, and ASan+UBSan builds each pass all 31 CTest cases. Detached native artifacts built outside the repository with GNU C 15.2.0 through explicit name, explicit path, `CC`, and fallback each print exactly `>>(1 2 3 4 5)` then `>>6` after source removal. Integrated Linux, Windows, and macOS CI remains required before handoff.

### 2026-07-19 — Freeze the Level 1 public documentation contract with executable journeys

- **Related issue:** [Issue #9](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/9)
- **Context:** The accepted Level 1 evaluator, REPL, runner, C emitter, and native builder were individually documented during implementation, but a new user still needed one accurate language/toolchain reference, one concise quick start, explicit Anka boundaries, and the planned release archive contract. Documentation also needed an automated guard against command or canonical-example drift on every supported Main CI platform.
- **Decision:** Keep the root README as the concise product overview, clean-checkout quick start, four-command journey, supported-target list, and planned release installation entry point. Make `doc/level1.md` the focused normative Level 1 language and toolchain reference. Use `examples/level1.bennu` as the single canonical two-expression user example. Register one platform-neutral CTest documentation smoke that checks the canonical source after platform-newline normalization and required public contract text, then executes the documented REPL, run, emit-C/compile/run, and explicit native-build/run journeys against that file with exact normalized logical output. Document the v0.1.0 archive names from Issue #2 and the archive-root executable-plus-`LICENSE` layout fixed by Issue #10, without claiming that the release already exists.
- **Alternatives considered:** Put every detail in the README; duplicate the example in a documentation-only fixture; use shell-specific smoke scripts; describe Anka compatibility broadly; or defer asset layout documentation until release publication. These were rejected because they make the entry page unwieldy, allow example drift, reduce cross-platform testability, promise unsupported syntax or architecture, or leave Issue #10 without the user-facing installation contract it must test.
- **Rationale:** One short entry point, one detailed reference, one canonical source file, and one executable CTest journey are the smallest coherent public contract. CMake process execution works on all supported CI hosts without shell interpretation, while exact source and output checks tie prose to accepted behavior rather than internal implementation details.
- **Consequences or follow-up:** Changes to Level 1 syntax, limits, commands, compiler precedence, supported targets, release names, or archive layout must update the documentation and its smoke contract together. Issue #10 must package exactly the documented three assets with `bennu` or `bennu.exe` and `LICENSE` at each archive root. The reference intentionally excludes broader Anka syntax, later language levels, release publication, and architecture compatibility promises.
- **Validation/evidence:** The new documentation test was first observed failing because the README lacked the Level 1 reference link. After the public contract was written, the focused test executes all four documented journeys against `examples/level1.bennu`, compiles emitted C11 with the configured platform C compiler, runs both generated executables, and compares exact logical output. Clean Release, strict no-exceptions, complete CTest, diff hygiene, and integrated Linux x64, Windows x64, and macOS arm64 Main CI remain required before handoff.

### 2026-07-19 — Gate v0.1.0 publication behind target-native verified archives

- **Related issue:** [Issue #10](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/10)
- **Context:** Level 1 is implemented and documented on three release targets, but short-lived CI binaries are not a safe release boundary. The final pipeline must prove the exact archive bytes on their native systems, reject stale or off-main revisions, keep manual dry runs non-publishing, and avoid exposing a partial public release when any target or verification step fails. Publication also needs enough permission to create a GitHub Release without granting that permission to ordinary CI or packaging jobs.
- **Decision:** Add a dedicated fixed-version workflow with two event paths: manual dispatch is accepted only for the exact live `main` SHA and produces one-day temporary artifacts, while only an exact `v0.1.0` tag whose commit is reachable from freshly fetched live `main` can reach publication. Each Linux x64, Windows x64, and macOS arm64 job asserts the observed OS/CPU, performs a clean Release build and complete CTest, runs the file runner, emitted-C compile/execute, and fallback native-build journeys, packages exactly the platform executable plus root `LICENSE`, inspects the archive, verifies Unix executable permission, extracts outside the source/build tree, and executes the extracted binary before upload. The publication job depends on the complete matrix, downloads those same tested archive bytes, creates a draft only after every target succeeds, re-downloads and byte-compares the exact three assets, and publishes only after the draft is complete. Workflow permissions default to `contents: read`; only the exact-tag publication job has `contents: write`.
- **Alternatives considered:** Publish directly from each matrix job; rebuild archives in the publication job; let manual dispatch optionally publish; use mutable workstation artifacts; upload a public release before all target jobs complete; overwrite existing release assets on rerun; or grant workflow-wide write permission. These were rejected because they permit partial releases, break the identity between tested and released bytes, weaken the separate authorization gate, depend on untracked state, hide cross-target failure, destroy mismatch evidence, or increase the impact of an ordinary build step.
- **Rationale:** A target-native matrix followed by one serialized publication job makes the data flow explicit: source revision to tested archive bytes to verified draft to public release. Exact live-main checks prevent stale dispatches and off-main tags. Temporary artifacts are only transport between jobs, and a one-job write grant is the least privilege that supports GitHub Releases without repository secrets beyond the automatic token.
- **Consequences or follow-up:** A successful dry run cannot create or modify a tag or Release because the write-capable job is event-gated to the exact tag. A controlled post-package failure proves required-job failure keeps publication unreachable. A matching complete draft can resume to publication and a matching published release is a no-op; mismatched metadata, names, or bytes fail without overwrite. A failed first publication may leave a non-public partial draft for explicit maintainer inspection/removal rather than automatic destructive repair. This issue does not push `v0.1.0`; Chef retains the separate production tag authorization after independent acceptance.
- **Validation/evidence:** Test-driven static coverage first failed for the missing fixed release workflow, then for the missing cross-platform dry-run/package contract, then for the missing draft/verify/publish topology, and finally for this diary record. The contract test checks exact triggers, target/archive matrix, smoke and extraction operations, one-day artifacts, one publication dependency, tag-only gating, single write-permission grant, draft-first upload, byte comparison, mismatch refusal, and publish-last behavior. Clean local builds/tests, strict checks, integrated Main CI, successful exact-SHA three-target dry run, controlled failing dry run, archive inspection, and negative tag/release side-effect evidence remain required before implementation handoff.

### 2026-07-19 — Specify deterministic scalar lifting for the language rewrite

- **Related issue:** [Issue #23](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/23)
- **Context:** The released bootstrap language has only signed 64-bit integers, one-dimensional integer arrays produced by `ioata`, hard-coded unary calls, and a count-only C representation that is correct only because every array root is a one-through-n sequence. The rewrite needs a small normative basis for Bool, Int, and Double scalars; typed homogeneous vectors; scalar extension; mixed numeric calls; empty-vector composition; and backend agreement. Compatibility with the bootstrap Level 1 language is not a product requirement.
- **Decision:** Add `spec/bennu-spec-0001-scalar-lifting.md` as the proposed rewrite contract and use `iota` as the structural rank-1 constructor. Define explicit elementwise versus non-lifting primitive metadata; minimum-promotion overload selection validated against ambiguity; unconditional IEEE 754 round-to-nearest, ties-to-even `Int -> Double` conversion; exact vector-length agreement without singleton broadcasting; typed empty results with zero scalar-kernel calls; and observable validation order of arity, type, shape, resources, then execution. Require the lowest failing lifted result index for domain errors, structured diagnostic context, canonical Bool/Int/Double/vector formatting, per-vector and per-evaluation logical element budgets, and differential agreement across direct evaluation, CLI execution, emitted C, and native output. Recommend one type tag per homogeneous vector with untagged typed storage, rank-1 shape derived from payload length, a validated container-aware primitive table, shared free-function application, and a generalized flat argument-index IR.
- **Alternatives considered:** Preserve Level 1 spelling or behavior; infer lifting from C++ function types; enumerate scalar/vector overload combinations; broadcast singleton vectors; select overloads by declaration order; allow value-dependent only-if-exact numeric promotion; store a scalar type tag on every vector element; allocate a dynamic shape vector before multidimensional arrays exist; rely on allocator exhaustion instead of logical resource budgets; leave failure precedence or parallel domain errors nondeterministic; or let the C backend reconstruct arbitrary vectors from length. These were rejected because compatibility is not required, implicit host coupling and combination enumeration invite semantic drift, singleton broadcasting obscures shape mistakes, declaration- or schedule-dependent results are unpredictable, value-dependent promotion makes type behavior depend on contents, per-element tags weaken homogeneity and layout, speculative shape storage adds cost without capability, uncontrolled allocation weakens data integrity, and count-only emission cannot preserve lifted values.
- **Rationale:** A single scalar kernel plus explicit container lifting gives Bennu a small coherent semantics while keeping data and behavior separate. Type-based overload selection works for empty vectors, exact shape agreement makes costs and errors visible, logical resource charging remains stable under optimization, and typed contiguous payloads preserve direct data access without object-oriented dispatch. A shared semantic boundary and backend differential tests prevent the evaluator, generated C, and native execution from becoming separate languages.
- **Consequences or follow-up:** The proposal deliberately supersedes, rather than evolves compatibly from, Level 1. Implementing it requires new issues for the value model and primitive registry, generic call parsing and flat IR, evaluation and resource accounting, canonical floating formatting, and a C backend capable of arbitrary typed vector values. A product execution profile must choose concrete resource limits, and separate scalar specifications must settle overflow, division, floating equality/NaN, and source literal syntax. Existing Level 1 documentation remains an accurate record of v0.1.0 until rewrite implementation replaces the shipped surface; this diary entry does not rewrite earlier decisions.
- **Validation/evidence:** Design review traced the existing tagged integer/vector value, unary node representation, explicit result errors, forward evaluator, formatter, and count-only C array emission before selecting the rewrite boundary. The initial spec validation confirms 20 sequential numbered sections, 118 balanced Markdown code-fence markers, all required promotion/error/resource/backend terms, no legacy constructor spelling in the new spec, and a clean `git diff --check`; implementation and runtime validation are deferred by Issue #23's documentation-only scope.

### 2026-07-19 — Centralize resource safety without an iota-specific cap

- **Related issue:** [Issue #23](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/23)
- **Context:** Follow-up review distinguished the mathematical meaning of `iota` from the bootstrap evaluator's one-million-element guard. A local primitive cap was sufficient when the sequence constructor was the only operation capable of allocating a large vector, but the rewrite adds vector literals, lifted results, filters, sorts, and future allocation-capable primitives. The initially proposed mandatory per-vector and cumulative produced-element limits would make an arbitrary deployment policy part of core semantics and would treat logical production as memory even when storage can be reused.
- **Decision:** Give `iota` no primitive-specific fixed element threshold in the core language. Require every vector-producing or workspace-allocating operation to pass through one evaluation resource boundary that performs index/count representability checks, overflow-safe payload and workspace byte calculations, active-profile checks, and complete-allocation preflight before scalar-kernel execution. Return structured `ResourceError` reasons for size overflow, active-profile refusal, or unavailable allocation, with no partial language value or host exception as normal control flow. Allow named execution profiles to define memory or work limits for a deployment, but do not require a logical produced-element budget; when a profile enables logical work charging, it must define optimizer-independent units and reset boundaries.
- **Alternatives considered:** Retain a fixed `iota` element cap; require every primitive to define its own size restraint; keep mandatory `max_vector_elements` and `max_produced_elements_per_evaluation` counters; make only memory limits mandatory; or rely entirely on the host allocator. Primitive-specific caps become incomplete and inconsistent as the language grows, a produced-element counter is a work/gas policy rather than an accurate memory measure, mandatory concrete limits unnecessarily restrict trusted local use, and unguarded host allocation can overflow sizing arithmetic, terminate the process, or expose host exceptions instead of an explicit Bennu failure.
- **Rationale:** Centralizing allocation safety covers all current and future data producers with one inspectable rule while keeping `iota[n]`'s meaning independent of an arbitrary number. Mandatory representability and transactional failure protect correctness and data integrity. Optional execution profiles let a local CLI, constrained embedding, and untrusted service choose different resource policies without turning those policies into primitive semantics.
- **Consequences or follow-up:** The resource section and Issue #23 acceptance criteria now distinguish core semantics, mandatory allocation safety, optional memory limits, and optional logical work accounting. Rewrite implementation must provide shared vector/workspace allocation functions or equivalent free-function boundaries and test multiple primitives through injected overflow, profile-limit, and allocation-failure paths. A later product-profile issue may select concrete shipped limits. The current bootstrap evaluator's existing one-million-element behavior is unchanged because Issue #23 changes only the proposed rewrite specification.
- **Validation/evidence:** Focused validation confirms the revised spec has 20 sequential numbered sections and 120 balanced Markdown code-fence markers; contains the central allocation boundary, overflow-safe byte calculation, transactional `ResourceError`, optional-profile, optional-work, large-`iota`, and multi-producer conformance requirements; contains none of the obsolete `ResourceLimit`, `max_vector_elements`, or `max_produced_elements_per_evaluation` terms; and passes `git diff --check`. The open Issue #23 body now carries matching no-magic-cap, shared-resource-boundary, and optional-work acceptance criteria.

### 2026-07-19 — Stage the scalar-lifting rewrite behind an atomic public cutover

- **Related issue:** [Issue #23](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/23)
- **Context:** BENNU-SPEC-0001 replaces assumptions embedded across values, errors, formatting, lexer/parser nodes, primitive dispatch, allocation, evaluation, C emission, native execution, tests, examples, and public documentation. Replacing all of those in one implementation issue would make independent validation and safe local-main integration impractical. Exposing the rewrite one command at a time would temporarily make the evaluator, runner, emitter, and native builder different languages. Open Issue #21 also targets the same evaluator API, CMake, and test boundaries and must not race the rewrite.
- **Decision:** Add `spec/bennu-spec-0001-implementation-plan.md` with eleven dependency-ordered work packages, WP0 through WP10. Resolve the spec's blocking syntax, scalar-domain, and execution-profile decisions and the relationship to Issue #21 before code work. Build typed values/errors/formatting, centralized resources, primitive metadata/kernels, lifting, generic syntax/IR, the integrated evaluator, and the typed backend as directly tested foundations while the bootstrap public journeys remain coherent. Select the C backend strategy through comparable measured evidence. Switch evaluator, CLI, C/native backends, examples, tests, and documentation together in WP10, then delete bootstrap code and temporary adapters. Limit the initial conforming primitive set to `inc`, `add`, `equals`, `not`, and `iota`; `length` remains illustrative and `divide` remains a domain-error example. Require every implementation package to become its own issue and dependency-gated Coder/Chef pair, with spec-to-test traceability and layer-specific validation.
- **Alternatives considered:** Implement the rewrite as one large issue; expose partially completed semantics through selected public commands; maintain a permanent Level 1 compatibility layer; let Issue #21 and rewrite foundation work proceed concurrently; choose constant-result embedding or runtime C lowering without measurements; or treat final cross-backend testing as a substitute for focused layer tests. These were rejected because they enlarge integration and diagnosis risk, permit public semantic drift, add compatibility complexity that the product does not require, invite shared-file conflicts and duplicated test topology, make cost-sensitive backend choices without evidence, or discover ownership/type/resource defects too late.
- **Rationale:** Dormant foundations let each data transformation and error boundary receive direct evidence while integrated `main` stays buildable. A single public cutover preserves one language across every shipped surface. Explicit dependencies, the Issue #21 gate, and serialized local-main integration keep ownership and scheduling visible. Focused tests plus final differential tests prove both local correctness and end-to-end semantic agreement.
- **Consequences or follow-up:** Issue #23 remains documentation-only and must be accepted before implementation issues branch from integrated `main`. Maintainers must create and prioritize the WP0 decision issues, resolve Issue #21, then create later work-package issues only as their prerequisites become ready. Temporary bootstrap adapters are permitted solely as short-lived migration scaffolding and are removed in WP10. Release publication remains separate after rewrite acceptance.
- **Validation/evidence:** The implementation plan contains 20 sequential numbered sections, balanced Markdown fences, explicit WP0 through WP10 headings, the required Coder-to-Chef graph, the Issue #21 dependency, initial primitive set, dormant-foundation/atomic-cutover strategy, test traceability, backend measurement gate, risk register, and completion definition. The companion links resolve locally in both directions, and `git diff --check` passes for the documentation changes.

### 2026-07-19 — Materialize the scalar rewrite dependency graph

- **Related issue:** [Issue #23](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/23)
- **Context:** BENNU-SPEC-0001's implementation plan defined planning labels WP0 through WP10, but implementation could not be scheduled safely until those labels had durable GitHub issue scopes, observable acceptance criteria, and explicit prerequisites. Issue #21 already owns the evaluator and test-boundary work that must finish before rewrite code touches the same seams.
- **Decision:** Map WP1 directly to existing Issue #21 rather than creating a duplicate. Split WP0 into decision Issues #24 through #26, and map WP2 through WP10 to implementation Issues #27 through #35. Make Issue #23 the parent specification for the complete graph and list Issue #21 as a direct prerequisite on every code-bearing rewrite issue, #27 through #35, even where that dependency would also be transitive. Keep all new issues open and undispatched until their prerequisites are accepted and the required dependency-gated `Implement -> Verify` cards are created.
- **Alternatives considered:** Create a second issue for WP1; rely only on transitive links to Issue #21; combine all WP0 decisions into one issue; combine the rewrite into one implementation issue; or create runnable Kanban cards while prerequisites remain unresolved. These were rejected because they duplicate ownership, obscure the shared-boundary gate, couple independently reviewable language decisions, make validation and integration too large, or allow work to start before its product dependencies are satisfied.
- **Rationale:** One durable issue per independently verifiable outcome gives each stage a clear branch, acceptance boundary, and Chef handoff. Repeating Issue #21 on every code issue makes the prerequisite visible to humans and automation without requiring dependency-graph traversal. Separating issue creation from card dispatch records the complete roadmap without bypassing readiness gates.
- **Consequences or follow-up:** Issues #24 through #35 are planning records, not authorization to implement. Issue #23 and each listed prerequisite must be accepted before the corresponding Coder card can run. Before dispatch, every implementation issue still requires the repository's explicit two-card Hermes graph, dedicated issue branch, serialized local-main integration, required CI, durable GitHub handoff, and independent Chef acceptance; no pull request is used.
- **Validation/evidence:** An authenticated GitHub audit confirmed Issues #24 through #35 are open with the intended titles, every new issue names parent Issue #23, and every code-bearing issue #27 through #35 explicitly names Issue #21 as a prerequisite. The implementation plan and Issue #23 carry the same issue map, and repository diff hygiene is checked after the documentation updates.

### 2026-07-19 — Define rewrite application and literal syntax

- **Related issue:** [Issue #24](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/24)
- **Context:** BENNU-SPEC-0001 fixed scalar-lifting semantics but deliberately left application and literal source syntax abstract. The implementation graph cannot build typed values or a generic flat parser until Bool, Int, Double, nonempty-vector, typed-empty-vector, line, delimiter, and diagnostic-span rules are normative. Anka demonstrates both explicit `add[1 2]` calls and the concise `inc 5` form; losing the latter would make the rewrite needlessly less expressive without resolving a real ambiguity.
- **Decision:** Add `spec/bennu-spec-0002-application-and-literal-syntax.md`. Use adjacent square brackets and whitespace-separated arguments for generic zero-, unary-, and multi-argument application, while preserving `p expression` as right-associative one-argument prefix syntax so `inc 5` and `inc inc 5` remain valid. Require brackets for multi-argument calls and parse unknown syntactically valid primitive names independently of the primitive registry. Define lowercase Bool literals; canonical signed-decimal Int literals without leading plus, leading zeroes, or `-0`; decimal or exponent-marked binary64 literals plus exact `inf`, `-inf`, and `nan`; exact-type scalar-only nonempty vectors in parentheses; and `Bool()`, `Int()`, and `Double()` typed empty vectors. Give the language no grouping parentheses, infix precedence, comments, commas, implicit arity-driven parsing, or vector-literal promotion. Define LF/CRLF logical records, multiline balanced delimiters, optional final newline, byte-based one-based positions, half-open spans, and deterministic primary spans for syntax and primitive diagnostics. Link the decision from BENNU-SPEC-0001 and its implementation plan, and update the semantic spec to use real typed-empty and unary-prefix source examples.
- **Alternatives considered:** Require brackets for every call and remove `inc 5`; allow whitespace application to consume a primitive's registered arity; accept `add 1 2` as a binary call; use comma-separated calls or vectors; use parentheses for both grouping and vectors; infer a bare `()` type from call context; promote mixed Int/Double vector elements; accept host-specific numeric spellings or conversion outcomes; or use visual tab expansion and host character indexing for diagnostics. Bracket-only syntax discards a compact and familiar unary form, registry-driven parsing couples syntax to mutable semantic metadata, implicit binary prefix calls and dual-purpose parentheses create harder boundaries, alternate separators expand a deliberately small language, contextual empty typing and vector promotion hide source types, host numeric behavior can vary by locale or library, and visual or host-dependent coordinates are not deterministic.
- **Rationale:** One syntactic argument for prefix application makes `inc 5` unambiguous and desugars directly to the same generic call representation as `inc[5]`; explicit brackets keep every multi-argument boundary visible. Exact literal and vector rules preserve data types before evaluation, while typed empty spellings make overload selection possible without context or element inspection. ASCII terminals, logical-record rules, and explicit byte spans give the future tokenizer, flat parser, evaluator, C backend, and conformance fixtures one portable source contract.
- **Consequences or follow-up:** WP2 can construct distinct empty vector types and canonical scalar values, and WP6 can tokenize every rewrite form into generic call nodes and a contiguous argument arena without one expression kind per primitive. `add 1` is a syntactically valid unary call that later fails arity validation, while `add 1 2` fails on trailing input; multi-argument source must use `add[1 2]`. A Double parser must provide correctly rounded, locale-independent, complete-token conversion and explicit finite-overflow failure. Existing Level 1 commands remain unchanged until the atomic WP10 cutover. Runtime implementation and executable behavior are outside this documentation-only issue.
- **Validation/evidence:** Focused documentation validation confirms 14 sequential top-level sections, 52 balanced fence markers, working companion and external links, every required literal/application/delimiter/span term, explicit `inc 5` and right-associative prefix examples, typed-empty source examples, Anka differences, no abstract typed-empty operand in a BENNU-SPEC-0001 source position, and a clean `git diff --check`. A clean Visual Studio 2026 Release configuration and build succeeded. The first CTest invocation exposed an uninitialized shell environment in which direct `cl.exe` smoke tests could not locate `inttypes.h`; rerunning under the installed Visual Studio x64 developer environment passed all 31 tests, including `documentation.level1_journeys`, emitted-C, native-build, and release-workflow contracts. Runtime rewrite implementation remains deferred to the dependency-ordered issues.

### 2026-07-19 — Define host-independent scalar domains for the rewrite

- **Related issue:** [Issue #25](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/25)
- **Context:** BENNU-SPEC-0001 fixed lifting, overload selection, and unconditional `Int -> Double` promotion but deliberately delegated the initial scalar kernels' boundary semantics. Typed-value, kernel, evaluator, and C-backend work cannot share one language unless signed overflow, binary64 rounding, nonfinite results, NaN equality and representation, precision loss, and structured domain failures are exact before implementation begins.
- **Decision:** Add `spec/bennu-spec-0003-scalar-domain-semantics.md`. Make Int `inc` and `add` checked mathematical operations that return `DomainError` with stable reason `integer_overflow` exactly when the result is outside the signed Int64 domain. Require strict IEEE 754 binary64 addition rounded directly with round-to-nearest, ties-to-even, preserving signed zero, infinities, gradual underflow, and subnormals without ambient-rounding, trap, excess-precision, reassociation, or fast-math dependence. Treat every Double arithmetic input combination as successful, including infinity and NaN results. Collapse all NaN encodings to canonical positive quiet NaN `0x7ff8000000000000` at Bennu value boundaries, make sign, payload, and signaling state unobservable, and define Double equality as numeric equality with signed zeros equal, same-signed infinities equal, and every NaN unequal to every value including itself. Confirm unconditional correctly rounded `Int -> Double` conversion, retained Bool/numeric separation, exact hexadecimal boundary vectors, structured overflow operands/signature/span/index context, and identical C++20/C11 obligations.
- **Alternatives considered:** Wrap or saturate signed overflow; inherit C or C++ overflow; treat floating overflow, infinity, underflow, or NaN as domain errors; preserve host NaN sign and payload; expose signaling behavior or floating status flags; compare Double bit patterns; make NaNs equal to themselves; promote Int only when conversion is exact; allow Bool/numeric coercion; rely on ambient rounding and default integer conversion; or validate floating results only through decimal formatting. Wrapping and saturation hide mathematical boundary failures, host signed overflow is not a portable language rule, rejecting IEEE results fragments Double arithmetic, payload propagation varies across operations and compilers, bit equality mishandles signed zero and NaN, value-dependent promotion destabilizes overload behavior, coercion weakens the small type system, host defaults can differ, and decimal output cannot prove exact binary64 agreement.
- **Rationale:** Checked Int arithmetic protects data integrity without enlarging the numeric domain. Strict binary64 plus one canonical NaN gives evaluator and generated C a finite, testable contract while retaining useful IEEE nonfinite values. Numeric equality matches ordinary arithmetic expectations, unconditional promotion keeps type behavior independent of contents, and exact bit fixtures expose backend drift that formatted text can hide. A single generic overflow reason remains stable because the primitive identity and structured operands already identify the operation.
- **Consequences or follow-up:** WP2 must canonicalize NaN at value construction and store structured scalar diagnostic fields. WP4 must use checked Int operations, strict binary64 kernels, and exact promotion rather than host defaults. WP9 must establish equivalent C11 compile and floating-environment conditions or reject an unsupported target. The implementation must preserve or restore an ambient rounding mode, prevent host floating traps from escaping, compare Double NaN by classification, and differentially test exact bits across evaluator and C/native paths. Integer division, floating ordering, approximate equality, bit casts, and source syntax remain outside this issue.
- **Validation/evidence:** Focused validation confirms 16 sequential top-level sections, 26 balanced fence markers, fixed-width binary64 hexadecimal vectors, ASCII content, working companion links, required scalar/error/backend terminology, no obsolete placeholder terms, and clean diff hygiene. An independent binary64 calculation checked every listed `Int -> Double` result and the finite signed-zero, ties-to-even, subnormal, and overflow addition vectors bit for bit. A Visual Studio 2026 Release build passed, and CTest under the Visual Studio x64 developer environment passed all 31 tests. Integrated three-platform Main CI remains required before handoff.

### 2026-07-19 — Ship trusted-local and bounded execution profiles for the rewrite

- **Related issue:** [Issue #26](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/26)
- **Context:** BENNU-SPEC-0001 centralized mandatory representability, overflow-safe sizing, complete-allocation, and no-partial-result rules but deliberately delegated which execution profiles Bennu ships, which optional limits exist, their units and reset boundaries, `ResourceError` presentation, cross-backend agreement, and deterministic fault-injection testing. WP3 resource-boundary implementation cannot begin until those deployment policies are exact, and a trusted local CLI, a constrained embedding, and an untrusted service legitimately need different resource policies.
- **Decision:** Add `spec/bennu-spec-0004-execution-profiles.md` and cross-link BENNU-SPEC-0001 section 12/section 20 and its implementation plan. Ship `trusted-local-v1` as the default CLI profile with `max_vector_bytes`, `max_live_evaluation_bytes`, and `max_work_units` all explicitly omitted while every mandatory safety check still applies, making `profile_limit` unreachable there. Define `bounded-v1` as deployment-configurable with no universal numeric defaults and at least one explicit cap, with profile identity and agreement covering the resolved configuration and fault injection kept orthogonal and test-only. Fix nonnegative integral units: canonical vector payload bytes with Bool 1, Int 8, Double 8; canonical live reservation bytes released only at defined logical lifetimes; and optimizer-independent logical work units charging 1 per scalar-kernel invocation, n per lifted n-element application, n for `iota[n]`, 0 for empty results, and additive nested charges even under fusion. Metadata, allocator overhead, spare capacity, validation, formatting, literals, and bookkeeping are uncharged; exact-limit charges succeed, one-past fails, and an omitted limit differs from zero. Give each complete program one resource context, with each REPL submission and each emitted/native invocation resetting independently and work accounting monotonic. Preserve BENNU-SPEC-0001 validation order; require `ResourceError` reasons `size_overflow`, `profile_limit`, and `allocation_unavailable` with structured context, stderr presentation, nonzero exit, and no partial output. Require identical canonical charge events and profile-refusal outcomes across evaluator, emitted C, and native paths for the same profile identity, with natural host allocation availability exempt and injected failures providing deterministic conformance evidence via synthetic counts, tiny caps, and a fail-at-reservation-ordinal seam.
- **Alternatives considered:** Impose a universal numeric memory or work cap on the default CLI profile; pick concrete default numbers for `bounded-v1`; treat profile agreement as name-only without the resolved configuration; charge physical bytes or optimizer-visible operations instead of canonical units; refund work for discarded values; keep one accumulating resource context across REPL submissions; make injected allocation failure a profile option; require bit-for-bit natural allocation-failure agreement across backends; or claim Anka-compatible resource behavior. These were rejected because arbitrary caps misclassify legitimate trusted local computation, universal numbers cannot fit divergent deployments, name-only identity lets differently configured executions claim false agreement, physical or optimizer-visible charging makes declared limits drift under fusion and storage reuse, refunds and shared contexts make refusal outcomes schedule-dependent, conflating test seams with policy weakens production configuration, host allocation availability is inherently nondeterministic, and Anka is inspiration rather than a compatibility target.
- **Rationale:** Separating mandatory allocation safety from optional deployment limits keeps core semantics small while letting each deployment choose its own policy. Canonical accounting units and admission-time charging make costs predictable and independent of implementation strategy, satisfying the plan's requirement that fusion or storage reuse cannot silently change a declared work profile. Exact-limit and zero-versus-omitted semantics remove boundary ambiguity, per-boundary context resets match user-visible evaluation units, and the identity-plus-configuration agreement contract gives differential tests one observable profile truth across all three execution paths.
- **Consequences or follow-up:** WP3 must implement the resource context, canonical charging, profile configuration, and the injected allocation-failure seam exactly as specified, and WP5/WP8/WP9 evaluator and backend work must replay identical charge events and refusal outcomes. Conformance suites must cover zero, one, exact-limit, one-past, arithmetic-overflow, allocation-unavailable, multiple-producer, no-kernel-execution, no-partial-result, and reset cases without dangerous host allocations. Future limits or semantics changes require new versioned profile names; the `-v1` contracts are frozen. Concrete `bounded-v1` numbers remain a deployment decision outside the language specification.
- **Validation/evidence:** Focused validation confirms the new specification has 12 sequential top-level sections and balanced Markdown fences, all four companion links resolve in both directions, required profile/limit/reason/agreement terminology is present, the obsolete short work-limit spelling from the earlier draft of BENNU-SPEC-0001 section 12.3 is gone repository-wide, and `git diff --check` is clean. The documentation smoke, full build, and complete CTest suite pass locally, and integrated three-platform Main CI on the merged `main` SHA remains required before handoff.

### 2026-07-19 — Clarify live workspace accounting and simultaneous-limit precedence

- **Related issue:** [Issue #26](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/26)
- **Context:** Independent verification of the initial execution-profile specification found that `max_live_evaluation_bytes` named vector reservations but did not unambiguously charge the generic `reserve_workspace(context, byte_count)` boundary. It also required checking every configured profile limit without specifying which limit supplies the singular `ResourceError` context when one admission exceeds several limits simultaneously. Those gaps could make conforming backends disagree on admitted workspace, refusal identity, and diagnostics.
- **Decision:** Amend BENNU-SPEC-0004 so every admitted typed vector payload contributes its canonical Bool 1, Int 8, or Double 8 payload bytes to live usage and every admitted generic workspace contributes exactly its requested `byte_count`, excluding physical layout and allocator overhead. Apply `max_vector_bytes` only to typed vector-payload reservations, including workspace explicitly declared as such, and never to generic workspace merely because of its use. After representability and overflow-safe sizing, impose exact simultaneous-limit precedence: `max_vector_bytes`, then `max_live_evaluation_bytes`, then `max_work_units`, followed by complete allocation. The first exceeded configured relevant limit supplies the single exact `profile_limit` context; a refused admission changes no usage counter and performs no allocation. Require conformance fixtures for all three limits, every relevant two-limit combination, and a generic-workspace-only live-byte refusal.
- **Alternatives considered:** Charge generic workspace only when it has a vector element type; use physical allocated bytes for generic workspace; subject every workspace reservation to `max_vector_bytes`; report every exceeded limit; let backend check order choose the reported limit; or partially commit earlier charges before a later limit fails. These were rejected because they respectively leave real workspace outside the live budget, make agreement depend on allocator strategy, broaden an individual-vector limit into a second general-memory limit, violate the singular structured error contract, create backend-visible nondeterminism, or make refusal non-transactional.
- **Rationale:** Charging the central boundary's exact logical requests covers every admitted payload and workspace while preserving backend-independent accounting. Separating typed vector payload from generic workspace keeps each configured limit's meaning coherent. Fixed precedence and transactional admission give evaluators and generated backends one deterministic refusal and exact context for the same profile identity.
- **Consequences or follow-up:** WP3 must classify reservations as typed vector payload or generic workspace, compute all prospective charges before mutating context state, enforce the fixed order, and commit counters only after every relevant profile check succeeds. Cross-backend tests must compare the winning limit and exact context as well as unchanged counters and absence of allocation on refusal.
- **Validation/evidence:** Focused validation confirms 12 sequential top-level sections, 16 balanced Markdown fences, ASCII content, five resolving relative links, all correction terms and all seven diary fields, and no obsolete `max_work` spelling. `git diff --check` is clean, the Visual Studio 2026 Release build passes, and CTest under the Visual Studio x64 developer environment passes all 31 tests. Required three-platform Main CI on the integrated `main` SHA remains part of the Issue #26 correction handoff.

### 2026-07-19 — Reject source/output aliases before creating output artifacts

- **Related issue:** [Issue #15](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/15)
- **Context:** `emit-c` and `build` accepted an output path naming the input source. Their publish-last replacement then atomically and successfully replaced the user's Bennu source with generated C or a native executable. Comparing only the supplied path text would miss relative/absolute spellings, `.` and `..` components, Windows case variants, and distinct names for the same filesystem object.
- **Decision:** After the source has been read successfully, pass both commands through one shared identity guard before evaluation, C emission, compiler selection or invocation, temporary-directory creation, and publication. First compare absolute lexically normalized paths; then use `std::filesystem::equivalent` to apply the host filesystem's identity behavior, including Windows case handling and existing aliases such as hard links. Treat a missing, lexically distinct output as a legitimate new destination, but fail closed with a path-identity diagnostic when an existing output's identity cannot be determined. Report an identified conflict as `error: source/output alias: output path refers to input source` and retain the existing same-directory atomic publication paths for distinct outputs.
- **Alternatives considered:** Compare raw path strings; compare only absolute lexical paths; use only `canonical` or `weakly_canonical`; defer identity checking until publication; or move the complete CLI publication machinery into a new abstraction. Raw or lexical-only checks miss filesystem aliases, canonical-only handling complicates legitimate nonexistent outputs, a late check permits needless compiler and temporary artifacts, and a larger publication refactor adds unrelated risk to a focused data-integrity fix.
- **Rationale:** The two-stage path-and-filesystem check is direct, dependency-free, and handles both nonexisting distinct destinations and existing object aliases. Running it at the earliest shared post-read boundary preserves established missing-source diagnostics while guaranteeing ordinary alias rejection before either command creates an artifact or launches a compiler.
- **Consequences or follow-up:** Source/output aliases now fail before semantic evaluation and output work; distinct existing outputs retain their prior atomic replacement behavior. The guard follows Bennu's current narrow command-line path policy. Broader Unicode-path support or adversarial filesystem race hardening would require separate issue-driven work rather than changing this bounded CLI contract implicitly.
- **Validation/evidence:** The process regression first reproduced successful exact-path source replacement and a changed SHA-256 hash. It now executes the built CLI for exact, relative/absolute plus `..`, hard-link, and Windows case-variant aliases across both commands; verifies nonzero status, exact diagnostics, unchanged source hashes, zero fake-compiler launches, and no C/build/publication leftovers; and confirms replacement of distinct existing C and native outputs. Clean GNU 15.2.0 Release, strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions`, and ASan+UBSan builds each pass all 34 CTest cases. Integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-19 — Make successful stdout completion explicit

- **Related issue:** [Issue #17](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/17)
- **Context:** The direct runner, help path, and REPL wrote through `std::cout` but returned success without checking stream state. A buffered write or final flush could therefore lose part or all of the user-visible result while automation observed status 0, unlike generated C, which already returns nonzero when its stdout operations fail.
- **Decision:** Route every successful CLI stdout write through two small internal free functions: one performs an unformatted byte write and checks stream state, and one explicitly flushes and checks stream state. `run` checks each complete formatted result and performs one final flush; help checks its complete fixed transcript and final flush; the REPL checks each prompt write and prompt flush plus every formatted result write. Any failure stops the path with status 1 and writes the best-effort diagnostic `error: unable to write stdout` to stderr. Do not enable stream exceptions.
- **Alternatives considered:** Rely on process-exit flushing; enable iostream exceptions; flush after every batch result; replace all CLI output with C stdio; or add a production environment-variable failure hook. Exit flushing remains unobservable to the return path, exceptions violate Bennu's explicit-error policy, per-result flushing adds unnecessary system calls, a full stdio conversion enlarges this focused fix, and a runtime test hook would expose test-only behavior in the shipped CLI.
- **Rationale:** One checked write boundary and one checked flush boundary expose the two distinct failure classes with direct control flow and no dependency or transcript change. The runner retains batched flushing, while the interactive prompt keeps the flush required before input. The helpers accept injected streams, giving every platform deterministic failure coverage without depending on a host-specific sink.
- **Consequences or follow-up:** A batch run can still have emitted a prefix before an external failure, but it can no longer report that partial stream as successful. The REPL terminates on prompt or result-output failure instead of continuing. Successful stdout bytes remain unchanged. Stderr diagnostics are intentionally best effort because the process cannot guarantee that a second output channel is writable.
- **Validation/evidence:** Before the fix, the built `run`, `--help`, and `repl` paths each returned 0 with empty stderr when redirected to Linux `/dev/full`; the new executable-level regression observes all three return 1 with the exact diagnostic. Portable injected stream-buffer tests cover immediate write failure, a partial later write after one successful result, and final-flush failure. Clean GNU 15.2.0 Release, strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions`, and ASan+UBSan builds each pass all 36 CTest cases; direct runner/help/REPL `/dev/full` probes and an equivalent generated-C probe return nonzero. Integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-19 — Represent deterministic stdout faults with plain operation data

- **Related issue:** [Issue #17](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/17)
- **Context:** Independent verification found that the portable stdout fault seam used three inheritance-based `std::streambuf` test classes with protected and private members. That representation exercised the intended failures but violated Bennu's repository-wide data-oriented C++ constraints.
- **Decision:** Preserve the existing `std::ostream` entry points and route them through a plain `OutputSink` containing context plus write and flush function pointers. A write operation reports explicit success and a byte count, so the shared checked-write function rejects both immediate failure and a short write. Portable tests use a plain state struct and free functions to select immediate write failure, one complete write followed by a partial write, or final-flush failure.
- **Alternatives considered:** Keep the `std::streambuf` subclasses as a test-only exception; pre-mark a standard stream as failed; or rely only on process redirection to a failing device. Test-only inheritance still violates the repository rules, pre-marking stream state cannot faithfully distinguish a short later write or a failing flush operation, and no single failing output device is available on every supported platform.
- **Rationale:** The sink is flat, explicit operation data with visible state and no inheritance, access controls, exceptions, environment hook, or dependency. The shipped CLI keeps its existing stream-facing calls and exact behavior, while the same checked-write boundary receives deterministic byte counts from portable tests.
- **Consequences or follow-up:** The internal helper gains one small indirect operation call per checked CLI output operation. CLI output is not a measured hot path, and this keeps failure injection outside shipped command-line controls. The Linux `/dev/full` executable contract continues to validate the real iostream adapter.
- **Validation/evidence:** The policy reproduction first rejected `tests/stdout_test.cpp` for `class`, `protected:`, and `private:` tokens; the corrected file contains none. The portable unit test passes immediate failure, one complete write followed by a short write, and a failed final flush. Clean GNU 15.2.0 Release, strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions`, and ASan+UBSan builds each pass all 36 CTest cases. Direct runner, help, and REPL `/dev/full` probes return 1 with the exact diagnostic, generated C returns 1, and the direct/generated differential passes. Integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-20 — Resolve POSIX PATH compilers before build-directory isolation

- **Related issue:** [Issue #18](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/18)
- **Context:** POSIX fallback discovery checked a bare compiler name against `PATH` in the caller's working directory, but the child repeated `execvp` lookup only after changing into Bennu's isolated build directory. A relative `PATH` entry therefore named a different directory at validation and launch time. Explicit `--cc` and `CC` bare names had the same launch-time dependency even though only fallback performed preflight discovery.
- **Decision:** On POSIX, scan `PATH` for a selected bare compiler while still in the caller's working directory, preserving colon-separated ordering and the empty-entry meaning of the current directory. When an executable candidate is found, convert that candidate to an absolute launch path before creating or entering the isolated directory. Keep the configured selection text and source separate from the resolved launch path so diagnostics still identify the original `--cc`, `CC`, or fallback selection. Continue passing the executable and compiler arguments directly to `execvp`; leave explicit paths containing separators and the Windows process path unchanged.
- **Alternatives considered:** Rewrite relative entries in the process-wide `PATH`; delay the child `chdir` until after launch; copy or link the compiler into the build directory; use a shell command; or open the executable and launch by file descriptor. Rewriting inherited environment state broadens side effects, removing the isolated working directory changes compiler behavior and cleanup boundaries, copying a toolchain executable is incorrect and incomplete, a shell violates inert argument handling, and descriptor-based launch adds portability and interpreter-script complexity not needed for this correctness fix.
- **Rationale:** Resolving the first executable candidate once at the existing selection boundary preserves the caller-directory meaning demonstrated by discovery and makes launch independent of the later working-directory transition. A separate launch string keeps diagnostics and selection precedence stable, adds no dependency, and leaves shell-free argument vectors and Windows behavior intact.
- **Consequences or follow-up:** POSIX bare-name selection now binds to the first executable candidate visible before temporary-directory creation. As with ordinary `PATH` lookup, filesystem mutation between resolution and `execvp` can still make that path fail; the existing selected-source process diagnostic, output preservation, and cleanup behavior then apply. No compiler flags, fallback retries, cache, or new public interface are introduced.
- **Validation/evidence:** The new fake-process contract first failed for relative-`PATH` explicit selection with `could not be started (OS error 2)`, then passed for bare names selected by `--cc`, `CC`, and fallback after pre-`chdir` resolution. Its relative directory contains spaces and shell metacharacters, the trace checks separate compiler arguments, and the sentinel proves the text remains inert. Clean GNU 15.2.0 Release, strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions`, and ASan+UBSan builds each pass all 36 CTest cases. A real `/usr/bin/cc` reached first through `PATH=toolchain:/usr/bin` builds and runs the canonical program with exact output and no temporary artifacts. Diff hygiene passes; integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-20 — Skip non-file candidates during POSIX compiler resolution

- **Related issue:** [Issue #18](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/18)
- **Context:** Independent verification found that the caller-directory resolver treated `access(candidate, X_OK)` as proof that a PATH candidate could be executed. POSIX reports a searchable directory as X_OK, so an earlier directory named like the compiler was frozen to an absolute launch path and failed with `EACCES`; direct PATH launch instead continued to the later executable candidate.
- **Decision:** Before accepting a POSIX PATH candidate, follow its filesystem status and require a regular file in addition to successful `access(..., X_OK)`. Continue scanning in PATH order after directories, missing entries, inaccessible metadata, and non-executable files. Preserve the existing absolute binding, original selection text and attribution, direct `execvp` argument vector, explicit path handling, and Windows behavior.
- **Alternatives considered:** Reject only directories; use `symlink_status`; or probe every candidate by forking and attempting execution. A positive regular-file requirement rejects other non-launchable file kinds as well as directories, ordinary status follows executable symlinks while `symlink_status` would incorrectly reject them, and execution probes would add processes, side effects, and race complexity to selection.
- **Rationale:** Regular-file status plus execute access is the smallest side-effect-free preflight that fixes the observed mismatch while retaining executable files and symlinks to them. It removes the faulty assumption at the shared resolver used by `--cc`, `CC`, and fallback without changing public data or process control flow.
- **Consequences or follow-up:** PATH ordering is preserved across filesystem entries that cannot represent an executable file, including searchable directories. As before, mutation after resolution or host execution restrictions not exposed by file status and access can still make the final `execvp` fail with the existing attributed diagnostic and cleanup behavior.
- **Validation/evidence:** The focused fake-process regression places searchable directories for both `relative-cc` and fallback `cc` in the first caller-relative PATH component and executable fakes in the second. It failed before the production change with attributed OS error 13 and passed after the shared resolver required a regular file for all three selection sources. Clean GNU 15.2.0 Release, strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions`, and ASan+UBSan builds each pass all 36 CTest cases. A real `/usr/bin/cc` reached through a symlink in the second caller-relative PATH component after an earlier `cc` directory builds and runs the canonical program with exact output and no temporary artifacts. Diff hygiene passes; integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-20 — Address draft Releases by creation ID until publication

- **Related issue:** [Issue #20](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/20)
- **Context:** The v0.1.0 publication script created a valid empty draft and then tried to recover its ID through `GET /releases/tags/v0.1.0`. GitHub defines that route for published Releases, so it returned HTTP 404 while the draft existed and stopped the first production attempt before asset upload. The same tag route was also used for draft metadata and asset verification, while the static release test incorrectly assumed a draft was visible there.
- **Decision:** Keep the tag route only for initial discovery of an already-published Release and final confirmation after publication. For a fresh Release, project the creation response to the ID, JSON ID type, tag, target commit, draft state, and prerelease state; require one nonempty numeric JSON ID plus the exact eligible metadata before any upload; and address all draft metadata and asset listing through `GET /releases/{id}`. Upload the exact three assets to that ID, recheck draft metadata and downloaded bytes, PATCH that ID only after every check passes, then switch back to the tag route to prove the Release is public and unchanged. Preserve the published matching-release no-op and mismatch refusal as separate non-mutating paths.
- **Alternatives considered:** Retry tag lookup until the draft appears; list all Releases and select a draft by tag; parse only the ID from the response and trust the remaining fields; use asset IDs from each upload as the only verification state; or automatically delete and recreate a partial draft. Polling cannot make a published-only route expose drafts, collection selection is broader and can be ambiguous, ID-only trust misses malformed or unexpected creation state, upload responses alone do not prove the complete remote set, and destructive recovery would erase failure evidence and exceed this issue's non-mutation scope.
- **Rationale:** The successful create response is the narrowest authoritative source of the draft identity, and the by-ID endpoint is valid across draft and published states. Explicit state-dependent endpoints make the publication transition visible, preserve publish-last ordering, and require no new release-runtime dependency or feature. Validating the JSON type as well as decimal text prevents a malformed string ID from being accepted accidentally.
- **Consequences or follow-up:** A failure after draft creation can still leave a non-public partial draft for maintainer inspection, but malformed creation metadata, upload failure, incomplete or extra assets, byte mismatch, draft metadata drift, and a failed publish request cannot reach a successful publication PATCH. A successful PATCH followed by failed public verification reports failure without attempting another mutation. Existing published v0.1.0/release 356339145 and its assets are not changed by this source correction. The Linux-only fake-API CTest requires Python 3 and Bash 4 because it exercises the same Bash implementation as the Ubuntu publication job; other target-native CTest jobs retain the static release contract, and the production job keeps its existing GitHub CLI and Git dependencies.
- **Validation/evidence:** The committed fake-API regression first reproduced HTTP 404 immediately after draft creation because the script queried the tag route. It now models draft invisibility until PATCH and passes the exact create, three-upload, by-ID metadata/byte verification, publish, and public-tag verification order. Focused failures cover missing, nonnumeric, string-typed, and mismatched creation fields; creation and upload failures; missing, extra, or byte-mismatched assets; metadata drift; publish failure; published no-op and mismatch refusal; pre-API provenance rejection; and failed post-publish lookup. Clean GNU 15.2.0 Release and strict warnings/conversions/Werror/no-exceptions builds each pass all 37 CTest cases; Bash syntax, Python byte-compilation, ShellCheck 0.11.0, actionlint 1.7.12, static workflow validation, staged security scanning, and diff hygiene pass. The first integrated run exposed macOS's Bash 3.2 lacking the production script's existing `mapfile` command, so the API regression was limited to the Linux publication platform; replacement integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-20 — Keep Windows paths UTF-16 at operating-system boundaries

- **Related issue:** [Issue #14](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/14)
- **Context:** The Windows CRT narrowed `main` arguments and `CC` through the active ANSI code page before Bennu saw them. The CLI then passed those lossy bytes through narrow stdio, filesystem construction, executable discovery, process creation, and atomic replacement APIs. Latin-1 paths available in the active code page could work while CJK and non-BMP paths became `?`, causing wrong-path access and lossy diagnostics across `run`, `emit-c`, and `build`.
- **Decision:** Enter the Windows executable and fake compiler through `wmain`, convert valid UTF-16 arguments and `CC` exactly once to internal UTF-8, and keep user-facing diagnostics and existing plain request records in UTF-8. Centralize explicit UTF-8/UTF-16 and UTF-8/`std::filesystem::path` conversion results in one dependency-free path-encoding component. At Windows OS boundaries use wide CRT and Win32 calls for source and temporary files, exclusive staging, atomic replacement, executable discovery, compiler logs, working directories, and direct compiler launch. Build the Microsoft-compatible quoted command line in UTF-16 and continue passing every argument directly to `CreateProcessW` without a shell. Leave POSIX native-byte paths and direct `fork`/`execvp` behavior unchanged.
- **Alternatives considered:** Change every public request and diagnostic field to `std::wstring` or `std::filesystem::path`; set only the process ANSI code page to UTF-8; use `std::filesystem::u8path` plus narrow CRT and Win32 calls; or invoke the compiler through PowerShell or `cmd.exe`. A Windows-wide public model would spread platform-specific text through otherwise portable data, an ANSI-code-page setting is inherited-environment policy rather than a complete filesystem/process boundary, wide filesystem objects cannot repair later narrow API calls, and command shells would reintroduce interpretation and argument-boundary risk.
- **Rationale:** UTF-8 keeps Bennu's portable command and diagnostic data compact and inspectable, while UTF-16 is retained exactly where Windows requires it. One explicit conversion boundary removes the root lossy assumption without adding a dependency or a second command model. Wide direct process launch preserves the existing shell-metacharacter and publish-last contracts while allowing source, output, temporary-parent, compiler, and environment paths to contain any valid Unicode scalar value.
- **Consequences or follow-up:** Windows diagnostics are emitted as UTF-8 bytes and the console output code page is selected as UTF-8; redirected output remains directly comparable. Invalid UTF-8 or unpaired UTF-16 is rejected explicitly instead of being replaced. The native-builder public API remains UTF-8 text and gains no flags, shell syntax, or fallback behavior. Temporary and replacement names append only ASCII suffixes to native filesystem names, preserving the original Unicode filename and same-directory atomicity.
- **Validation/evidence:** Test-first Main CI run [29708127333](https://github.com/tuncbahreadingmaterial-ops/Bennu/actions/runs/29708127333) passed Linux x64 and macOS arm64 but failed the new Windows CJK/non-BMP `run` case with `éß 文 🐍` already narrowed to replacement characters, reproducing the reported root cause. The Windows-only contract covers exact missing-path diagnostics, Unicode `run`, Unicode input/output and atomic `emit-c` failures, Unicode temporary/output parents, explicit and `CC` compiler paths, compiler argument traces, compiler-failure sentinel preservation, and temporary cleanup. Focused Linux native-build, alias, run, and emission regressions pass after the shared path changes; clean Release, strict, sanitizer, full Windows, integrated Main CI, and final hygiene gates remain required before handoff.

### 2026-07-20 — Statically link the Windows release runtime

- **Related issue:** [Issue #16](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/16)
- **Context:** The published Windows archive contains only `bennu.exe` and `LICENSE`, but the executable imports `MSVCP140.dll`, `VCRUNTIME140.dll`, and `VCRUNTIME140_1.dll`. A machine without a sufficiently recent Visual C++ Redistributable can therefore reject every Bennu command at loader startup, while the public documentation names only the external C11 compiler used by `bennu build`.
- **Decision:** Build every MSVC target with CMake's `MultiThreaded` runtime (`MultiThreadedDebug` for Debug configurations), making the packaged Release executable self-contained with respect to the Microsoft C and C++ runtime. Keep the two-file archive layout. During Windows packaging, inspect the staged executable with `dumpbin /DEPENDENTS`; reject all `MSVCP*.dll` and `VCRUNTIME*.dll` imports and every other direct dependency that is neither a Windows API-set name nor a DLL present in the Windows system directory. Exercise the extracted package with a system-only `PATH` and no `CC`: `--help`, `repl`, `run`, and `emit-c` must succeed, while `build` must fail only with the existing missing-fallback-compiler diagnostic. Support Windows 11 x64 or newer for this release contract.
- **Alternatives considered:** Keep dynamic linkage and require users to install a matching-or-newer x64 Visual C++ Redistributable; bundle the redistributable installer; or deploy runtime DLLs app-locally beside `bennu.exe`. A machine-wide prerequisite preserves Microsoft servicing but leaves launch dependent on an installation step, bundling an installer enlarges the release and requires elevation and lifecycle handling, and app-local DLLs change the fixed archive layout while shifting runtime servicing into the package without removing deployment files.
- **Rationale:** Static linkage is the smallest supportable launch contract for a two-file command-line archive. It removes the undeclared loader dependency, requires no installer or runtime-DLL packaging, keeps non-build commands independent of all developer tooling, and can be enforced against the exact staged PE before archive creation.
- **Consequences or follow-up:** The Windows executable grows and carries its linked runtime implementation until Bennu is rebuilt and republished; Microsoft runtime security or correctness fixes therefore require a new Bennu binary rather than a machine-wide Redistributable update. Redistribution is covered by the Visual Studio toolchain's static-runtime redistribution terms, but release maintainers remain responsible for rebuilding with supported patched tools. Windows operating-system DLLs and API sets remain external platform dependencies. The external C11 compiler remains a build-only prerequisite and the archive remains exactly `bennu.exe` plus `LICENSE`.
- **Validation/evidence:** The release workflow contract first failed because no `CMAKE_MSVC_RUNTIME_LIBRARY` policy existed. Target-native Windows build, staged-PE import output, exact archive inspection, extracted package journeys, execution on a Windows 11 x64 host without Visual Studio or a C compiler, full cross-platform CTest, and integrated Main CI remain required before handoff.

### 2026-07-20 — Dynamically link the Windows release runtime

- **Related issue:** [Issue #16](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/16)
- **Context:** The static-runtime approach recorded immediately above would make future builds self-contained, but it cannot correct the already-published v0.1.0 archive without moving the immutable release tag or replacing published asset bytes contrary to the accepted release pipeline. Issue #16 requires the verified package hash to match the durable GitHub Release asset. That asset imports `MSVCP140.dll`, `VCRUNTIME140.dll`, and `VCRUNTIME140_1.dll` and was built with MSVC runtime 14.51.36231.0.
- **Decision:** Supersede the static decision before integration. Explicitly select CMake's `MultiThreadedDLL` runtime (`MultiThreadedDebugDLL` for Debug configurations) and require the Microsoft Visual C++ 2015-2022 Redistributable (x64), version 14.51.36231.0 or newer, before any Windows command can launch. Link the official evergreen x64 installer and Microsoft compatibility guidance. Keep the two-file archive layout. During Windows packaging, require exactly the three observed MSVC runtime DLL imports, allow only Windows system or API-set dependencies beyond them, verify the installed x64 runtime registry version, hide all developer-tool paths and clear `CC`, then require `--help`, `repl`, `run`, and `emit-c` to succeed while `build` fails only because `cl.exe` is absent.
- **Alternatives considered:** Statically link the runtime; replace the published asset under the existing tag; publish a new release; bundle the redistributable installer; or deploy runtime DLLs app-locally. Static linkage cannot change the durable published artifact, replacing it violates the accepted no-overwrite release contract, a new release is outside this issue and requires separate authorization, bundling adds an installer and elevation lifecycle, and app-local deployment changes both published bytes and the fixed archive layout.
- **Rationale:** An explicit dynamic prerequisite is the only policy that makes the existing published v0.1.0 bytes supportable without mutating release history. The minimum-version rule follows Microsoft's binary-compatibility contract, package inspection prevents the documented import set from drifting silently, and the clean-path smoke proves that the C11 compiler remains a `build`-only prerequisite rather than a launch requirement.
- **Consequences or follow-up:** Every Windows command depends on a machine-wide x64 Redistributable that users or administrators must install and service. Microsoft can service that shared runtime independently of Bennu, and future Bennu builds may raise the minimum required version when their MSVC toolset advances. Release CI must update the documented minimum and import contract deliberately when that happens. The archive remains exactly `bennu.exe` plus `LICENSE`; no compiler, installer, or runtime DLL is bundled.
- **Validation/evidence:** The dynamic-policy release workflow contract first failed against the static generator expression. The published v0.1.0 archive hash and PE imports are recorded in Issue #16. Target-native package inspection, runtime-version verification, compiler-free extracted-package journeys, exact published-asset hash comparison, full cross-platform validation, and integrated Main CI remain required before handoff.

### 2026-07-20 — Support the durable Linux archive on Ubuntu 24.04 x64

- **Related issue:** [Issue #13](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/13)
- **Context:** The immutable v0.1.0 Linux archive was built on Ubuntu 24.04 and requires symbols through `GLIBC_2.34` and `GLIBCXX_3.4.32`. Its published SHA-256 is `21f4608d1e32f83dcaf6ad121c2bac51181077330fcd8e5af31388e0504b85e6`. An Ubuntu 22.04/GCC 12-era promise would be broader than those durable bytes can satisfy, while moving the tag or replacing the asset would violate the accepted release contract. Linux packaging also had no pinned build input, no symbol/dependency ceiling, and no execution of downloaded archive bytes on a declared oldest userland.
- **Decision:** Define the v0.1.0 portable Linux floor narrowly as Ubuntu 24.04 LTS x64 or newer, with glibc 2.39 or newer and an Ubuntu-supported Linux 6.8 GA or HWE kernel. Build Linux archives in the GA x86-64 `noble-20240423` OCI image pinned by index digest `sha256:562456a05a0dbd62a671c1854868862a4687bf979a96d48ae8e766642cd911e8`, install exact GCC 13.2/CMake 3.28.3/Ninja 1.11.1 packages from the signed Ubuntu snapshot `20240424T000000Z`, and retain dynamic linkage. Reject binaries above `GLIBC_2.34` or `GLIBCXX_3.4.32`, or whose resolved runtime graph differs from `libstdc++.so.6`, `libgcc_s.so.1`, `libc.so.6`, and `libm.so.6`. Download the produced archive into separate GA-floor and current Ubuntu 24.04 jobs, then extract and execute `--help`, `run`, strict `emit-c`, and `build` plus atomic failure probes before publication can run.
- **Alternatives considered:** Promise Ubuntu 22.04 by rebuilding with GCC 11/12; statically link libstdc++/libgcc; replace the published v0.1.0 asset; promise only raw symbol versions across arbitrary distributions; or keep the mutable `ubuntu-24.04` hosted runner as the build boundary. Rebuilding for 22.04 would create bytes unlike the durable release and cannot repair v0.1.0, static linkage changes package bytes and servicing behavior, replacement violates immutable release evidence, raw versions omit tested distribution/kernel integration, and a mutable runner can silently raise the ABI floor.
- **Rationale:** A narrow distribution promise is the only honest floor compatible with the immutable release. Pinning both the OCI userland and signed package snapshot makes future builds deliberate and repeatable, while ceilings set to the observed durable requirements detect a compiler/library drift even though Ubuntu 24.04 provides newer runtime interfaces. Dynamic linkage preserves the published layout and makes the required system libraries explicit.
- **Consequences or follow-up:** Ubuntu 22.04 and arbitrary mixed userlands are not supported by the v0.1.0 archive even if some individual symbol requirements appear sufficient. Future releases may lower the floor only through a separately versioned archive built and measured on that older userland. Snapshot or image retirement requires a deliberate policy update, new measurements, documentation, and a new diary entry. The first certificate bootstrap disables HTTPS peer verification only while APT verifies signed snapshot metadata; the installed pinned CA bundle is then used for a fresh verified update before toolchain installation.
- **Validation/evidence:** A fresh local user-namespace execution of the GA Ubuntu 24.04 image and `20240424T000000Z` snapshot installed the exact workflow package set and measured GNU C++ 13.2.0, CMake 3.28.3, Ninja 1.11.1, glibc 2.39, highest binary requirements `GLIBC_2.34` and `GLIBCXX_3.4.32`, direct `NEEDED` entries `libstdc++.so.6`, `libgcc_s.so.1`, and `libc.so.6`, and resolved libraries adding only `libm.so.6`. Controlled `GLIBC_2.36`, `GLIBCXX_3.4.33`, and extra-library fixtures fail with explicit diagnostics. All 39 tests pass in the current and pinned release userlands; all 39 pass with strict warnings; all 37 sanitizer-compatible tests pass, with the two release-ABI checks intentionally excluded because sanitizer instrumentation adds `libasan` and `libubsan`. The durable archive hash matches `21f4608d1e32f83dcaf6ad121c2bac51181077330fcd8e5af31388e0504b85e6` and its four journeys plus atomic failures pass on both current and floor userlands. Actionlint 1.7.12, ShellCheck 0.10.0, syntax checks, and diff hygiene pass. Integrated Main CI and a non-publishing release dry run remain required before handoff.

### 2026-07-20 — Limit the v0.1.0 Linux support promise to Ubuntu 24.04 LTS

- **Related issue:** [Issue #13](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/13)
- **Context:** The preceding Issue #13 decision described Ubuntu 24.04 LTS x64 "or newer," but the release workflow exercises the pinned Ubuntu 24.04 GA userland and the current supported Ubuntu 24.04 hosted userland. It does not execute the archive on a later Ubuntu release, and its documented GA/HWE kernel rule is specific to Ubuntu 24.04. Verification also found that the floor journey's `gcc` installation used `--no-install-recommends` without the libc development headers required to compile Bennu's emitted standard C.
- **Decision:** Narrow the v0.1.0 portable Linux support target to supported Ubuntu 24.04 LTS x64 userlands, without promising later Ubuntu releases. Keep the pinned GA image as the oldest userland and the current `ubuntu-24.04` hosted runner as the serviced current userland. Install the exact `libc6-dev` package explicitly in both the build and floor-runtime jobs. Publish a Linux archive name only after its staged bytes pass layout, mode, license, and ELF checks; failures remove the same-directory temporary archive and leave no caller-visible output.
- **Alternatives considered:** Add an Ubuntu 26.04 or rolling-distribution job; retain "or newer" as an untested compatibility inference; rely on `gcc` to recommend `libc6-dev`; or create the final archive before post-write checks and delete it on known failures. A newer release is not yet part of the v0.1.0 contract, an untested breadth claim is not support evidence, recommended packages are deliberately skipped, and cleanup-after-publication creates a visibility window and can miss unexpected exits.
- **Rationale:** An exact distribution release promise matches the two environments CI actually exercises. Explicit headers make the strict emitted-C and native-build journeys reproducible under `--no-install-recommends`. A complete same-directory temporary followed by an atomic no-replace link makes verified bytes visible in one filesystem operation and preserves the existing refusal to overwrite an archive.
- **Consequences or follow-up:** Supporting a later Ubuntu release requires a deliberately exercised environment and a new compatibility decision. The package script now requires the output directory to support ordinary hard links, as the supported Linux filesystems and GitHub runners do. Arbitrary local Bennu ELFs may require symbol versions below the ceilings; tests record their measured maxima rather than requiring equality, while the durable fixture and hash retain exact v0.1.0 evidence.
- **Validation/evidence:** The floor-header contract and controlled second-ELF-failure cleanup regression failed before the correction. Required branch, pinned/current userland, static-analysis, integrated CI, and durable GitHub evidence remain to be recorded after execution.

### 2026-07-20 — Internalize evaluator stages and compile focused doctest tests beside implementation

- **Related issue:** [Issue #21](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/21)
- **Context:** Bennu's tokenizer, parser, flat evaluator representation, and primitive kernels were exposed from `include/bennu/evaluator.hpp` only so separate hand-written test executables could call implementation stages. The public token and program records therefore admitted impossible source states and required defensive correspondence, index, kind, and root-order checks, including a second full tokenization during parsing. A shared `bennu_core` library existed primarily to link those tests even though Bennu ships only one executable. The three focused suites each implemented their own runner, and there was no pinned third-party test framework or no-exception framework policy.
- **Decision:** Vendor doctest v2.5.3 at exact upstream commit `2d0a9359a60c51affe2a9bebb1be1dca47868151`, retain its MIT license beside the header, and verify both vendored files by SHA-256 in an architecture contract. Compile one `bennu_tests` executable from the same implementation source set as production plus one doctest implementation/main translation unit. Put evaluator, C-emitter, and native-builder test cases beside their implementation so they can exercise translation-unit-local stages without making those stages public. Define `DOCTEST_CONFIG_DISABLE`, `DOCTEST_CONFIG_NO_EXCEPTIONS`, and `DOCTEST_CONFIG_NO_EXCEPTIONS_BUT_WITH_ALL_ASSERTS` on the production target; omit only the disable macro from the test target. Keep only source locations, public errors and values, public result records, `evaluate_expression`, `evaluate_source`, and `format_value` in the evaluator header. Make tokens, flat program nodes, stage result records, tokenization, parsing, primitive application, program evaluation, and the `ioata` retained-element limit internal. Trust parser-produced flat programs instead of revalidating unconstructible public input, lex each source buffer once, and pass one remaining retained-element budget into `ioata` before allocation; this one bound enforces both the one-result and whole-program million-element limits while retaining the source-visible diagnostic contract. Retain native compiler fallback preflight because the platform-fallback missing-compiler diagnostic is a documented and black-box-tested user contract; process launch remains the authoritative second check after selection.
- **Alternatives considered:** Fetch doctest during CMake configure; keep three standalone custom runners; keep `bennu_core` and link doctest tests to it; expose internal stages through a test-only public header; use conditional source inclusion instead of compiling implementation sources independently; retain redundant parser/evaluator validation; or remove native executable preflight and report only launch failure. Configure-time fetching makes ordinary and release builds network-dependent, multiple runners preserve boilerplate and inconsistent policy, a shared library changes the requested independent-compilation topology, a test-only public header still creates a second API surface, conditional source changes can hide production/test divergence, defensive checks for unreachable states keep the second lex and invalid-program category alive, and removing preflight would intentionally change a stable negative-path diagnostic outside this issue.
- **Rationale:** Translation-unit-local tests give focused access without exporting construction paths that production never accepts. One pinned, offline test framework and one implementation/main provide deterministic discovery and consistent assertions. Independently compiling the same source list proves both the production-disabled and test-enabled configurations. Removing unreachable-state checks simplifies the hot evaluation path while preserving every source-reachable category, location, and message. A decreasing retained-element budget makes the allocation invariant explicit and checks it before reserve or population.
- **Consequences or follow-up:** Production and tests compile the implementation sources separately by design, increasing test-build compilation work but preventing a shared target from masking compile-definition differences. Production source files include the vendored doctest header, so the disable and no-exception definitions are part of their build contract and the pinned header/license hashes must change deliberately during upgrades. Failed fatal assertions abort a no-exception test process rather than unwinding to the next test case. Native fallback selection keeps the existing lookup-versus-launch race and diagnostic distinction. There is no migration for users because the removed evaluator stages were implementation scaffolding rather than a supported language surface.
- **Validation/evidence:** The architecture contract first failed against the old missing-vendor/library/public-header topology. Focused doctest and architecture tests pass. A fresh GCC 15.2 Release build passes all 38 CTest tests, including evaluator CLI diagnostics, documentation journeys, release workflow, Linux ABI, Linux package, and publication-state contracts. The `-fno-exceptions` build passes all 34 unit, architecture, stdout, CLI, native-build, and documentation tests; its two Linux release-ABI/package probes are intentionally not applicable because disabling unwind support changes the direct `NEEDED` set, while the ordinary Release probes pass. The ASan+UBSan build passes all 36 compatible tests with the two Linux release-ABI/package probes intentionally excluded because sanitizer instrumentation adds `libasan` and `libubsan`, matching the established release-validation policy. The optimized production binary contains no doctest symbols or embedded test names. Diff hygiene, integrated Main CI, and the durable Issue #21 handoff remain required before completion.
- **Cross-platform correction:** The first integrated Windows build exposed MSVC's requirement for a complete `std::ostream` while doctest rendered a captured `std::string_view`; explicitly including `<ostream>` fixed compilation. The next Windows run built and passed the doctest suite but showed that checkout line-ending conversion changed the byte hash of the vendored header. The hashed doctest header and license are therefore marked `text eol=lf` in `.gitattributes`, preserving exact upstream bytes and the same pin check across checkout platforms.

### 2026-07-20 — Keep the contributor contract repository-local

- **Related issue:** [Issue #22](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/22)
- **Context:** The contributor agreement mixed Bennu's observable repository and GitHub workflow with details of an external task-running, notification, and scheduling setup. Those operational details were not part of Bennu's source, build, test, or contribution surface and made the agreement harder to reuse without that setup.
- **Decision:** Keep `AGENTS.md` limited to product direction, C++ and architecture constraints, testing and performance evidence, decision recording, and repository-local issue, branch, validation, integration, CI, handoff, verification, correction, and closure rules. Remove task-runner roles, dependency mechanics, notifications, and checkout scheduling. Preserve the Anka design reference and Bennu's no-pull-request local-main workflow.
- **Alternatives considered:** Retain the mixed workflow as historical context; replace it with another named automation system; or remove the complete branch and verification section. These options were rejected because they preserve external coupling, merely substitute one coupling for another, or discard repository-local requirements contributors still need.
- **Rationale:** A contributor contract should state rules that can be understood and observed from the checkout and Bennu's GitHub repository. Operational tooling can enforce those rules without becoming part of the versioned product documentation.
- **Consequences or follow-up:** External work coordination must carry its own operational protocol. Repository contributors retain the same issue branch, validation, local-main integration, CI evidence, open-issue handoff, independent verification, and issue closure expectations. No language, build, test, release, or runtime behavior changes.
- **Validation/evidence:** `AGENTS.md` is inspected for the retained product, architecture, test, performance, Anka, and diary rules; searched for the removed operational terms; checked for heading and link integrity; and included with this append-only diary entry in documentation-only diff-hygiene checks.

### 2026-07-20 — Represent evaluator values with typed scalars and homogeneous buffers

- **Related issue:** [Issue #27](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/27)
- **Context:** The bootstrap evaluator represented values as either one signed integer or a signed-integer array. Rewrite execution requires a dormant but testable value foundation for Bool, Int, and Double scalars and homogeneous rank-1 vectors, structured semantic failures that callers need not recover by parsing diagnostic text, and one canonical byte representation for values across the interpreter and later execution engines. Adding later scalar domains to the bootstrap record would either put redundant tags beside every vector element or leave inactive payload and empty-vector identity ambiguous.
- **Decision:** Represent a value as one explicit scalar-or-vector container tag, a directly tagged scalar payload, and a vector payload containing an element-type tag plus separate contiguous `uint8_t`, `int64_t`, and `double` buffers, exactly one of which is active. Store booleans as bytes rather than `std::vector<bool>`, preserve typed empty vectors through the vector element tag, derive rank and length from the container and active payload, and expose free functions for construction, validation, scalar projection, element type, rank, length, destruction, and formatting. Construction rejects non-binary boolean bytes, clears inactive fields, and normalizes every NaN to one quiet positive binary64 bit pattern; validation makes the same active/inactive and canonical-NaN invariants observable. Format finite doubles with the standard shortest-round-trip `std::to_chars` general conversion, then normalize exponent spelling; spell `nan`, `inf`, `-inf`, and negative zero explicitly; append `.0` to finite integral spellings with neither point nor exponent. Extend plain error data with optional primitive name, one-based argument position, expected and actual shapes, zero-based element index, resource context, and domain context including selected scalar signature and operands. Permit error construction from category and location alone so semantic data never depends on rendering presentation text first, while retaining existing public error categories, locations, messages, and diagnostics. Adapt only the existing integer bootstrap evaluator and C emitter to the new representation; do not add rewrite execution, new parsed literals, primitive dispatch, profile accounting, or C emission for dormant Bool/Double values.
- **Alternatives considered:** Use `std::variant` for every scalar and vector element; store one tagged scalar record per vector element; use `std::vector<bool>`; represent vectors as raw bytes with an external stride; use locale-sensitive streams or `std::to_string` for doubles; add a third-party decimal conversion library; or keep semantic context only in diagnostic strings. Per-element variants and tags add redundant storage and branching, `vector<bool>` is packed proxy storage rather than one-byte contiguous elements, raw bytes weaken type and lifetime invariants, streams and `to_string` do not define the required shortest canonical binary64 spelling, a new conversion dependency is unnecessary with the supported C++20 libraries, and parsing presentation text is unstable and loses typed operands and limits.
- **Rationale:** Separate typed buffers keep the vector hot path contiguous and untagged while a small outer tag preserves element identity, including for empties. Plain structs and free transformations keep ownership, inactive fields, and failure paths inspectable. Boundary normalization makes NaN equality-by-bits and formatting deterministic without changing finite values or signed zero. One canonical formatter prevents the interpreter, diagnostics, and later engines from independently choosing byte spellings. Optional structured error fields let current and future stages carry semantic facts without forcing cold-path data into successful values.
- **Consequences or follow-up:** A `Value` contains all three vector headers, although only one owns storage; this fixed outer overhead avoids per-element overhead and keeps destruction ordinary and explicit. Public plain records can be mutated into invalid states, so construction functions are the supported boundary and `validate_value` exists for checks and tests. `destroy_value` releases owned buffers and leaves a valid false scalar. The bootstrap parser still accepts only signed integer literals and existing primitives, and the C emitter relies on that evaluator invariant; dormant Bool/Double execution and bounded-profile allocation accounting remain later issue work. Cross-platform CI must prove the supported standard libraries produce the pinned shortest finite spellings.
- **Validation/evidence:** A link failure for the first typed-scalar construction test established the missing-foundation baseline. Embedded tests then cover all scalar and empty-vector tags, byte boolean storage, inactive-payload rejection, boolean validation, canonical NaN normalization and rejection, constant-time rank/length/projection boundaries, explicit destruction, exact scalar/vector spellings, and binary64 subnormal, normal, maximum, infinity, signed-zero, exponent-normalization, and parse-back round trips. The existing evaluator suite additionally checks structured `inc` type/domain and `ioata` resource context while all public Level 1 formatting and diagnostic contracts remain byte exact. A fresh GNU 15.2 Release build passes all 38 CTest cases; strict `-Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror -fno-exceptions` passes all 36 applicable cases; and ASan+UBSan with leak detection passes all 36 compatible cases. The two Linux release ABI/package tests are intentionally excluded from strict and sanitizer runs under the established instrumentation policy. Direct interpreted and native canonical journeys match the expected output byte-for-byte, and the direct overflow journey returns status 1 with its exact existing diagnostic and no stdout. The optimized production binary contains no doctest symbols or embedded new test names. Exact changed-file review and diff hygiene pass; integrated Linux, Windows, and macOS Main CI remains required before handoff.

### 2026-07-20 — Keep binary64 parse-back fixtures within the platform compatibility floor

- **Related issue:** [Issue #27](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/27)
- **Context:** The first integrated Main CI run, [29731253559](https://github.com/tuncbahreadingmaterial-ops/Bennu/actions/runs/29731253559), proved two portability gaps in the newly embedded formatting tests. AppleClang 17 with the Xcode 16.4 libc++ supplies floating-point `std::to_chars` but not floating-point `std::from_chars`, so production formatting compiled while the parse-back fixture did not. MSVC 19.44 also required a complete `<ostream>` declaration when doctest captured a `std::string_view` expected value.
- **Decision:** Keep production shortest formatting on `std::to_chars`, but parse the fixture's finite canonical strings with `std::strtod`, require complete input consumption, and compare the parsed binary64 bits with the original fixture bits. Store expected fixture spellings as string literals and include `<ostream>` explicitly for doctest capture rather than depending on transitive standard-library headers.
- **Alternatives considered:** Remove parse-back coverage; conditionally disable it on Apple platforms; add a third-party floating parser; or raise the macOS toolchain floor. Removing or gating the check weakens accepted cross-platform evidence, a new parser is unnecessary for test-only verification, and raising the platform floor would expand Issue #27 beyond its accepted scope.
- **Rationale:** `strtod` is present across the existing C++20 platform floor and is sufficient for a test that starts in the default C locale, verifies complete consumption, and compares exact binary64 bits. The production formatter remains locale independent and unchanged. Explicit standard-library includes make each doctest translation unit self-contained on MSVC.
- **Consequences or follow-up:** The parse-back fixture depends on the test process retaining its default C numeric locale; no Bennu test currently changes it. The test continues to cover minimum subnormal, normal boundaries, maximum finite, signed zero, and exact round trips without creating a new runtime dependency. Replacement three-platform Main CI is required before handoff.
- **Validation/evidence:** Run 29731253559 failed only at compilation on Windows x64 and macOS arm64, while Linux x64 passed. The correction requires the focused doctest, complete Release suite, strict no-exception suite, sanitizers, diff hygiene, and replacement integrated Main CI.

### 2026-07-20 — Make public value consumption checked and allocation-linear

- **Related issue:** [Issue #27](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/27)
- **Context:** Independent review of the merged foundation reproduced public plain `Value` records with unknown container and scalar tags, inactive payloads, invalid Bool bytes, and noncanonical NaNs. Validation rejected those records, but metadata accessors and formatting could map them to valid-looking values, while projection could return an invalid scalar as success. The same review measured 1,011 allocations while formatting 1,000 maximum-width Int values and 3,011 allocations for 1,000 Double values because each element created temporary strings. It also found that the bootstrap `ioata` retained-element cap was labeled as a rewrite `profile_limit` without the limit kind required by BENNU-SPEC-0004.
- **Decision:** Make element-type, rank, and length accessors return `ValueValidationResult` and write their result only after the complete public record validates. Make formatting return `ValueFormattingResult` with explicit invalid-value or conversion failure and no formatted bytes on failure. Make public scalar projection validate before reading any discriminator or payload. Keep future high-throughput projection free to use a translation-unit-local validated path after one validation pass rather than weakening the public boundary. Format Int and Double elements directly into one reserved output string with stack `to_chars` buffers and normalize exponents while appending, without one string allocation per element. Keep the existing Level 1 `ioata` error category and diagnostic, but do not attach rewrite `ResourceErrorContext` to that legacy retained-element policy. Retain the current by-value success/error result layout until the later evaluator integration issue establishes error ownership and provides a representative benchmark; adding heap indirection to this dormant foundation solely to reduce `sizeof` would trade visible storage for cold-path allocation without measured user impact.
- **Alternatives considered:** Document that callers must validate manually; return sentinel metadata or invalid-looking text; trust each projected element without checking the complete record; introduce a forgeable validated-view token; allocate temporary strings per formatted element; reinterpret the Level 1 element count as a byte or work profile limit; or heap-allocate every error context now. Manual validation and sentinels leave the public API easy to misuse, partial checks permit operations on globally invalid values, a public plain validation token cannot prove the underlying record was not mutated, per-element allocation makes formatting cost needlessly depend on element count, the legacy cap is neither a BENNU-SPEC-0004 byte nor work limit, and speculative error indirection changes ownership and failure costs without an evaluator workload.
- **Rationale:** One checked public boundary makes malformed plain data fail explicitly and preserves one invariant definition. Stack conversion plus a single conservatively reserved buffer keeps formatting data flow linear and inspectable. Separating the bootstrap limit from rewrite profile data avoids publishing structurally incomplete semantic context. Deferring the result-layout change keeps this correction focused on reproduced correctness and allocation defects while preserving evidence for the evaluator work package that owns hot-path application.
- **Consequences or follow-up:** Metadata access and standalone public projection now include full validation, which scans Bool and Double vector payloads. A later lifting implementation must validate each argument once and use a non-public validated projection loop to avoid quadratic work. Existing evaluator and CLI values come from constructors, check formatting success explicitly, and retain their exact stdout. Malformed formatting returns no partial text. Rewrite resource accounting remains deferred to its accepted work package, and any success/error representation change requires measured evaluator evidence.
- **Validation/evidence:** The pre-correction no-exception harness printed `unknown-scalar projection-ok=1 format=''`, `noncanonical projection-ok=1 validation-ok=0`, and `unknown-container rank=1 length=1 format='(1)'`. The corrected harness reports validation, metadata, projection, and formatting all fail with zero formatted bytes for unknown scalar, unknown container, and noncanonical NaN records. Embedded regressions also cover inactive payload and invalid Bool bytes. On Linux x64 with GCC 15.2.0, the fresh Release suite passes 38/38, the warnings-as-errors `-fno-exceptions` suite passes 36/36 applicable tests, and the ASan+UBSan suite passes 36/36 applicable tests. A no-exception allocation probe formats 1,000 minimum Int64 values into 21,001 bytes with one allocation and 1,000 maximum-magnitude finite Double values into 24,001 bytes with one allocation, down from 1,011 and 3,011 allocations respectively in the review baseline. Direct REPL, runner, overflow, type, and allocation-limit journeys preserve exact stdout, diagnostics, and exit status. Diff hygiene and replacement integrated Main CI remain required before handoff.

### 2026-07-20 — Centralize rewrite reservations in a transactional resource context

- **Related issue:** [Issue #28](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/28)
- **Context:** The typed value foundation used `std::vector` payloads, whose recoverable allocation failures cannot be converted to Bennu `ResourceError` in the strict no-exception build. The rewrite specification also requires vector payloads, typed or generic workspace, and optional logical work to share overflow-safe accounting, fixed profile-limit precedence, deterministic failure injection, and all-or-nothing admission. The still-public bootstrap language must retain its one-million-Int-vector behavior until atomic cutover without making that threshold an `iota` semantic rule.
- **Decision:** Add one plain `EvaluationResources` context with exact `trusted-local-v1` and `bounded-v1` identities, optional canonical vector-byte, live-byte, and work-unit limits, monotonic work and reservation-ordinal counters, and a zero-based allocation-failure injection ordinal. Route typed vector payload and generic workspace reservations through shared free functions that compute canonical sizes with checked multiplication and cumulative addition; check vector, live, and work limits in that order; obtain complete storage with `malloc`; and commit live/work counters only after allocation succeeds. Represent each active typed payload as one move-owned `unique_ptr` using `free` plus an explicit logical count, preserving typed empties, homogeneous untagged elements, and visible single ownership without `std::vector` allocation exceptions. Use fixed Bool 1, Int 8, and Double 8 accounting widths with compile-time physical-width checks. Keep work charging optional and optimizer-stable, refund only live bytes at explicit release, and never refund work. Preserve the temporary Level 1 retained limit by configuring the bootstrap evaluation context with an 8,000,000-byte individual/live limit and mapping its refusal back to the existing Level 1 diagnostic; the shared rewrite boundary itself contains no primitive-specific element cap.
- **Alternatives considered:** Catch `std::bad_alloc` around `std::vector`; rely on process termination in no-exception builds; use `new[]` separately for each typed payload; retain an `ioata` element counter beside the resource context; charge generic workspace against the individual-vector limit; commit counters before allocation and roll them back; or make fault injection part of a profile. Exception handling contradicts the required build and explicit-error policy, termination is not a recoverable Bennu result, separate allocation paths invite drift, a primitive counter preserves the rejected semantic cap, generic workspace has different normative accounting, rollback is harder to audit than commit-last admission, and test faults are instrumentation rather than deployment policy.
- **Rationale:** One preflight and one allocation seam make failure precedence, charge state, and ownership locally inspectable. Move-owned typed buffers preserve contiguous data while allowing allocation failure to return ordinary structured data. Overflow checks and commit-last admission guarantee that every refused request leaves counters and result state unchanged, and ordinal injection exercises the same path without destabilizing the host. Expressing the bootstrap bound as profile bytes keeps current journeys exact while the dormant rewrite remains uncapped under its trusted profile.
- **Consequences or follow-up:** `Value` is now move-owned rather than copyable, and its fixed outer record contains three nullable typed owners plus counts although only one payload is active. Callers transfer values explicitly; destruction and resource-release helpers leave inert valid records. Physical storage currently matches the canonical widths and is asserted at compile time. WP5 must charge the documented scalar/lifted/`iota` work at this boundary and prove zero kernel calls after preflight refusal; WP7 must place release calls at evaluator liveness boundaries; WP9 must reproduce the same canonical events in emitted/native execution; and WP10 removes the temporary bootstrap profile adapter. Concrete `bounded-v1` limits remain deployment configuration, not language constants.
- **Validation/evidence:** Test-first work observed missing resource APIs fail at link time before implementation. Embedded resource tests cover zero, one, exact, one-past, implementation-count and byte-size representability, multiplication and cumulative overflow, vector/live/work precedence including every relevant simultaneous pair, typed-vector versus generic-workspace accounting, ordinal allocation failure on first and later reservations, unchanged counters and absent partial values on refusal, live-byte release without work refund, trusted omission of policy caps, and independent context reset. The bootstrap evaluator routes `ioata` storage through the boundary and retains exact individual and cumulative one-million-element behavior, including the legacy refusal for a maximum Int64 request. On Linux x64 with GCC 15.2.0, a fresh Release build passes 38/38 tests, the warnings-as-errors `-fno-exceptions` build passes all 36 applicable tests, and the ASan+UBSan build passes all 36 applicable tests with leak detection and halt-on-error enabled. Direct REPL, runner, emitted-C, and native journeys preserve exact output; a one-past bootstrap allocation refuses with status 1 and no stdout. Production-symbol, security/debug-residue, architecture-rule, and `git diff --check` scans are clean. Integrated Main CI remains the required integration gate.

### 2026-07-20 — Reject malformed execution-profile records before admission

- **Related issue:** [Issue #28](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/28)
- **Context:** Independent RED verification mutated the public plain `EvaluationResources` record into bounded-v1 with no configured limit, trusted-local-v1 with configured limits, and an unknown profile tag. The three admission entries assumed that profile identity and configuration remained consistent: vector and workspace requests could reach injected allocation failure and advance the reservation ordinal, while zero-work charges could succeed. BENNU-SPEC-0004 fixes `ResourceError` to exactly the three reasons `size_overflow`, `profile_limit`, and `allocation_unavailable`, none of which describes a malformed execution context.
- **Decision:** Add the distinct generic `ErrorKind::invalid_execution_profile` and validate the public profile/configuration pair at the start of `allocate_vector`, `reserve_workspace`, and `charge_work` through one shared helper. Accept trusted-local-v1 only when all three optional limits are omitted, accept bounded-v1 only when at least one limit is configured, treat configured zero as present, and reject every unknown tag. Return fixed diagnostics with the supplied source location and producer context, no `ResourceErrorContext`, no partial reservation, and no accounting or ordinal change. Keep `make_bounded_resources` returning plain data because admission-time validation remains mandatory after public mutation or unknown-tag injection.
- **Alternatives considered:** Add or repurpose a `ResourceErrorReason`; validate only in resource constructors; normalize malformed records to a known profile; or validate inside the existing preflight after vector sizing. A fourth or repurposed resource reason would violate the specification's stable exactly-three contract, constructor-only checks cannot protect mutable public data, normalization would silently change deployment policy, and later preflight validation would let malformed identity lose precedence to size classification.
- **Rationale:** Malformed contexts use a non-`ResourceError` error class to preserve the specification's exactly-three stable `ResourceError` reasons. One earliest-boundary check makes refusal deterministic across allocation, workspace, and logical-work events while leaving every valid profile-limit, overflow, allocation, and charging path unchanged.
- **Consequences or follow-up:** Error-kind presentation now names invalid execution profiles explicitly. Future admission entries must reuse the same validation boundary, and public profile/configuration records remain inspectable plain structs rather than acquiring hidden construction invariants. The execution-profile specification is unchanged.
- **Validation/evidence:** The RED focused doctest reported 1 failed case, 95 assertions, and 20 failures, including ordinal advancement and successful malformed zero-work charges. After correction, the malformed-profile focus passes 1/1 with 270/270 assertions, the neighboring-valid-profile focus passes 1/1 with 14/14 assertions, and the full doctest binary passes 36/36 cases with 1,094/1,094 assertions. Regressions cover exact error kind, absent resource context, deterministic messages for all three malformed classes, preserved source and producer context, configured-zero trusted limits, invalid-before-sizing precedence, zero side effects, no partial vector/workspace, and valid adjacent configurations. A fresh Release build passes 38/38 CTest tests; a warnings-as-errors Release build with conversions and `-fno-exceptions` passes all 36 applicable tests; and an ASan+UBSan Debug build passes all 36 applicable tests with leak detection and halt-on-error. Direct REPL, runner, emitted C11, and native journeys preserve exact output; one-past and maximum-Int64 bootstrap requests refuse with status 1 and no stdout; dormant rewrite `iota[5]` syntax remains rejected. Diff, security, architecture, production-symbol, and changed-path hygiene plus integrated Main CI remain required before handoff.

### 2026-07-20 — Make primitive dispatch static, validated, and binary64-deterministic

- **Related issue:** [Issue #29](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/29)
- **Context:** WP4 requires a dormant but complete primitive foundation before lifting and resource preflight are connected in WP5. The rewrite needs stable identities, explicit container-aware signatures, deterministic exact-or-promoted overload selection, and scalar kernels whose Int64 and binary64 outcomes do not depend on undefined host overflow or caller floating-point state. The production executable must retain its existing Linux dependency contract, so libc/libm floating-environment calls cannot be introduced merely to mask traps and select round-to-nearest.
- **Decision:** Define explicit `PrimitiveId` values for `inc`, `add`, `equals`, `not`, and structural `iota`; represent source names, lifting mode, parameter/result container and scalar types, and implementation dispatch in static-lifetime arrays; and validate the production table once through a function-local result before either public evaluation entry proceeds. Reject duplicate identities or names, absent or duplicate signatures, zero-arity elementwise signatures, missing or mismatched implementation entries, vector-valued elementwise signatures, and any equal-minimum overload under the exact-cost-zero/Int-to-Double-cost-one policy. Select by total cost with no source-order tie break, then convert projected Int scalars to binary64 with an integer bit-construction routine implementing round-to-nearest-ties-to-even without consulting ambient rounding. Dispatch scalar kernels by descriptor identity plus the selected signature. Return checked Int overflow as structured `DomainError` with stable primitive identity, name, signature, exact operands, and call location. Canonicalize every successful scalar result through the value constructors. For Double addition, preclassify NaNs and opposite infinities, then temporarily install strict round-to-nearest, masked-trap, gradual-underflow state directly in x86-64 MXCSR or AArch64 FPCR/FPSR, restore the caller state exactly, and compile the kernel translation unit with strict floating-point options.
- **Alternatives considered:** Dynamically build descriptor objects at startup; infer overloads from C++ callable types or take the first compatible signature; materialize promoted vectors; use host casts for Int-to-Double; rely on the process floating-point defaults; call `feholdexcept`/`fesetround`; implement all binary64 addition in software; or connect vector lifting and structural `iota` now. Dynamic objects and callable inference hide lifetime and dispatch costs, source-order ties make metadata order semantic, vector conversion belongs after WP5 preflight, host casts and ambient defaults do not prove the normative rounding contract, libc floating-environment calls add a forbidden direct `libm` dependency on Linux, full software addition is disproportionate when supported targets expose the required control registers, and early lifting would cross the issue boundary.
- **Rationale:** Flat immutable tables make the complete primitive surface inspectable and allocation-free per descriptor. One startup validation converts authoring mistakes into an explicit implementation/configuration failure before source evaluation. Costed selection plus projection-time scalar conversion preserves exact overload preference without allocating, while checked integer arithmetic and controlled binary64 execution make every scalar kernel locally explainable and deterministic. Direct target control keeps the existing packaged-binary dependency set unchanged.
- **Consequences or follow-up:** The primitive registry and kernels are production-linked but remain dormant for source syntax; the bootstrap `inc` and `ioata` evaluator behavior is unchanged. WP5 must perform container classification, broadcast planning, reservation, and one validated projection/conversion pass before invoking these kernels, attach vector element indexes to propagated domain errors, charge kernel work, and apply structural `iota` outside scalar dispatch. Supported floating-point targets are now explicitly x86-64 and AArch64; CI must continue compiling the strict source options on Linux and Windows. Invalid production metadata is surfaced as `invalid_primitive_table`, a generic implementation error outside ordinary source errors.
- **Validation/evidence:** Test-first slices first failed at link time for the registry validator, selector/converter, and kernel entry before each implementation was added. Embedded fixtures cover all required descriptor failure classes, every Bool/Int/Double conversion pair, Cartesian primitive overload inputs, exact-first promotion and runtime ambiguity, Int64 conversion boundary bits, all normative integer and binary64 kernel vectors, canonical NaN and signed-zero behavior, structured overflow context, invalid dispatch, caller rounding restoration, flush-mode isolation, and an enabled overflow trap. On Linux x64 with GCC 15.2.0, a fresh Release build passes 38/38 CTest tests and 44/44 doctest cases with 1,556 assertions; the primitive-focused run passes 8/8 cases with 462 assertions; the warnings-as-errors, conversion-clean, `-fno-exceptions` Release build passes all 36 applicable tests; and the ASan+UBSan build passes all 36 applicable tests with leak detection and halt-on-error. Direct REPL inputs `1`, `inc 41`, and `ioata 3` preserve outputs `1`, `42`, and `(1 2 3)`. Linux compatibility and package checks confirm that strict floating-point control adds no direct binary dependency. Integrated Main CI and independent post-merge verification remain required.

### 2026-07-20 — Apply scalar lifting and structural iota through one resource-preflight path

- **Related issue:** [Issue #30](https://github.com/tuncbahreadingmaterial-ops/Bennu/issues/30)
- **Context:** WP4 supplied static primitive descriptors, deterministic scalar overload selection, projection-time Int-to-Double conversion, and scalar kernels, but intentionally stopped before container conformance, result reservation, repeated kernel execution, and structural `iota`. WP5 requires every lifted call to use one deterministic arity/type/shape/resource/domain order, preserve typed empties without invoking a scalar kernel, return no partial vector, and reuse the WP3 accounting boundary without restoring a primitive-specific element cap.
- **Decision:** Add one `apply_primitive` free function with a plain application context containing the shared `EvaluationResources` reference and an observable scalar-kernel invocation count. For elementwise descriptors, validate arity and actual element types once, select one scalar signature before shape agreement, classify the result as scalar or vector, require equal lengths for every vector argument without singleton expansion, reserve the complete typed result and logical work before execution, then project, convert, invoke, and store each result in increasing index order. Broadcast scalar arguments by projection rather than materializing vectors; on a lifted domain failure, attach the lowest zero-based result index and release the result reservation while retaining admitted work. For structural descriptors, require exact declared container and element types; implement `iota(Int scalar)` outside scalar dispatch as the typed Int vector `1..n` for positive `n` and typed empty for non-positive `n`, using the same checked reservation seam and no semantic element cap. Extend structured errors with supplied/accepted arities and actual/accepted value-type signatures so validation failures remain inspectable without parsing messages.
- **Alternatives considered:** Add per-primitive scalar/vector branches; materialize broadcast or promoted vectors; treat length-one vectors as scalars; run kernels before result admission; implement `iota` as a scalar kernel or reuse the Level 1 one-million-element threshold; return a partially filled vector on domain failure; or expose only invocation behavior indirectly. Per-primitive paths duplicate conformance and precedence, materialization adds avoidable allocation and copying, singleton expansion contradicts the accepted shape rule, late admission permits observable kernel work before refusal, `iota` changes cardinality and therefore is not elementwise, the bootstrap threshold is migration policy rather than language semantics, partial results violate all-or-nothing application, and an explicit counter makes zero-call preflight and empty behavior deterministic to test.
- **Rationale:** One flat application path keeps data movement, admission, invocation order, and failure cleanup visible. Selecting before shape fixes result type for typed empties and makes type failures outrank shape failures. Reserving once before the loop proves resource refusal has zero kernel side effects, while projection-time conversion preserves exact overload preference without temporary vectors. Structural `iota` shares validation and resources while remaining outside scalar lifting.
- **Consequences or follow-up:** The rewrite application API remains dormant until evaluator cutover, so Level 1 `inc` and `ioata` syntax and output are unchanged. Admitted vector work is not refunded after a later domain failure, matching the monotonic WP3 work counter, while live result bytes are refunded. Application tests are compiled beside the implementation and use the invocation count as instrumentation. Future evaluator integration must own result release at value-liveness boundaries and route source calls through this API rather than reimplementing conformance.
- **Validation/evidence:** The first scalar application test failed at link time before the shared entry existed; the first vector-broadcast test then observed one scalar result instead of three lifted results; and the first structural `iota` test reached invalid scalar dispatch. Focused regressions now cover scalar/scalar, scalar/vector, vector/scalar, equal vector/vector, strict singleton mismatch, Int-to-Double promotion including the 2^53 boundary, Bool results, typed empties, zero kernel calls on empties and preflight failures, deterministic arity/type/shape/resource/domain precedence and context, lowest-index domain failure with no partial vector, positive/non-positive `iota`, rejected structural lifting, size/profile/allocation failures, and deterministic small-shape pointwise properties. A complete Debug CTest run passes 38/38 tests; strict, sanitizer, diff-hygiene, integrated Main CI, and independent verification remain required.
